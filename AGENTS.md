# 项目协作规则

本文件是 Codex 在本项目中的规则入口。Cursor 使用 `.cursor/rules/*.mdc`，两套规则表达相同的项目约束；新增或修改规则时，必须同步维护本文件和对应的 Cursor 规则文件。

## 开发分支

- 不强制只在 `master` 上开发。功能较大、需要并行或计划建议隔离时，可以开功能分支、Git worktree 或 Cursor worktree；小改动可直接在 `master`（或当前分支）上改。
- 已在某条功能分支或某个 worktree 里开工：继续在该目录、该分支上改，不要擅自切回 `master` 把半成品写进主干。新分支 / worktree 用清晰命名（如 `feat/workflow-form`）。
- 功能分支或 Cursor 云端修 bug 分支（如 `origin/cursor/critical-bug-management-*`）合进本地 `master` 时优先 `git merge --no-ff`，保留来源分支和 merge 记录；不要 cherry-pick。使用者给的是 commit SHA 时，先找到包含它的远程分支再 merge。未要求时不要 push、不要删除远程分支、不要删除 worktree。

## 实现原则

- 只实现当前需求，保持代码简单、直接，避免提前设计无用的抽象和扩展点。
- 能复用现有模块就优先复用；只有在代码确实变大或职责明确时才拆分新层。
- 注释只说明不明显的原因，不重复描述代码行为。

## 编码与中文

- 文档和代码一律使用 UTF-8（无 BOM），不要按系统默认的 GBK / ANSI 读写。
- 新建、修改、从计划拆 brief、生成 diff 之后，检查中文是否仍是正常汉字；`docs/`、规则文件、源码字符串和注释、`.superpowers/sdd/` 都要查。
- 出现 `鏍囩`、`璇峰～鍐`、`å¤æ` 这类字，或中文读不通，就是乱码：以正确中文源（计划、规格、现有源码）为准改回来，不要把乱码写进实现。纠正编码，不要为此删文件。

## 前端规则（`front/`）

- Vue 单文件组件按 `template`、`script setup`、`style` 顺序组织。
- 优先使用 Element Plus（Vue 组件库）组件；没有对应能力时才使用原生控件。
- 可见文案使用普通 HTML 标签，不使用 `el-text`。
- 间距和对齐使用普通 HTML 元素配合 Flex 样式，不使用 `el-space`。
- 布局优先使用 Flex；只有 Flex 无法稳定实现时才使用 CSS Grid（网格布局）。
- 相邻 `el-button` 已有 `margin-left` 间距，按钮容器不要额外设置 `gap`。
- 新增或修改 `el-dialog` 时默认开启 `draggable`，除非需求明确不需要拖拽。
- 模板和 HTML 属性中不写行内 JavaScript（JavaScript，脚本语言）；事件逻辑抽到 `script setup` 函数中。
- 使用图标前必须确认 `@element-plus/icons-vue` 中有对应导出，再导入后使用；不要用 `Number`、`Document` 等 JavaScript（JavaScript，脚本语言）或浏览器全局名当作图标。
- 日期、时间的解析、格式化和起止计算优先使用 dayjs；复用 `front/src/utils/timeValue.js`，不要手写补零拼接或把 `YYYY-MM-DD` 交给 `new Date` 当 UTC。
- 改动相关代码时，保留用户已有的注释、`console.log` 等日志输出；除非用户明确点名删除或修改。

## 删除文件

- 删除任何文件之前，必须先列出具体路径并得到使用者确认。没有确认就不能删。
- 包括 Delete 工具、`rm`、`del`、`Remove-Item`、`git rm`、移入回收站。
- 「清理一下」「这些没用了」「乱码文件不该留」不够；确认必须点名路径。
- `.superpowers/`、其他 agent 的工作区文件、未跟踪文件同样要先问，不能因为「临时文件」或「编码错误」就直接删。

## 浏览器测试

- **未经使用者明确批准**，不要调用浏览器工具做测试、验证或点页面。
- 包括 Cursor 浏览器标签，以及打开页面、点击、截图、读页面结构这类操作。
- 改完界面后不要自己打开本机页面去点、截图、当收尾验证。
- 批准必须点名要用浏览器，并说明测哪一段（例如「用浏览器测关联子表单占整行」）。「验证一下」「测一下」「做完了」都不够。
- 未获批准时：看代码、跑单测，或等使用者本地点过。

## 日志文件

- 用户自行添加的日志文件（如 `*.log`、`logs/`、`*.log.*`）禁止删除、清空、覆盖或移动。
- 只有用户明确指定具体日志文件并要求处理时，才可以修改或删除。

## Git 提交

- 提交标题和正文优先使用中文。
- 提交信息要具体说明修改内容、原因，以及用户可见行为或配置变化。
- 避免使用“更新代码”“修复问题”“杂项调整”等无法反映实际内容的笼统描述。
- 用户未明确要求时，不要在每次功能改动后自动执行构建或 Git 操作。

## 手工测试用例

- 对照界面执行的用例只放 `docs/testcases/`。规格和计划仍在 `docs/superpowers/`。
- 实现**新**表单组件，或**已有**组件的核心功能发生改变后，**同一轮工作里**更新测试用例，不要只写规格。不是新组件也不能跳过：查询时机、选项来源、校验、能否进标签页/子表、入库展示、联动、权限这类用户可测行为变了，就要改对应用例。只改样式、文案措辞、不影响测法的内部重构，不必为了对齐而扩写用例。
- 至少三处：该能力的专项文件（没有就新建；核心行为变了就改旧编号或补新编号，不要留过期期望）、组合用例（当前为 `docs/testcases/2026-08-31-complex-form-manual-tests.md`）、纲领 `docs/testcases/README.md`（目录、调色板中文名与 `type` 对照、执行顺序、准备、破坏性步骤、不要测）。
- 用例里写控件时同时写调色板中文名和字段 `type`（例如子表单 `subform`、下拉单选框 `select`），不要只写简称。
- 联合测试必须考虑与已有复杂能力的互动：标签页 `tabs`、子表单 `subform`、数据联动 / 选择数据 `data`、流水号 `serialNumber`。包括能否放进标签页或子表、未打开的页签是否仍校验/联动、数据管理列表会不会多出不该有的列、校验失败是否跳到对应页签、删除父容器后子内容是否一起没。
- 调色板占位、控件未完成的，不要写成可测功能；在组合用例能力范围和 README 里标明不要当功能测。
- **面向人写，不要规格黑话**：设计、计划、用例都按界面语言写（详见下方「面向人的文档」）。

## 面向人的文档

设计文档、实现计划、测试用例、使用说明都是给人读的，不是给解析器看的。

**准确和易懂同样重要**；行为写对但没人读得懂，等于没写好。

适用范围（没有「设计/计划可以更偏规格」的例外）：

- 设计文档：`docs/superpowers/specs/`
- 实现计划：`docs/superpowers/plans/`
- 测试用例：`docs/testcases/`
- 使用说明：`docs/guides/`

接口路径、库表字段、源码文件名可以出现，但不能只写这些。先写界面上叫什么、人怎么点、会看见什么。

- **步骤**：按实际点击顺序写，不要只写内部状态（「当前行为空」「未就绪」）。
- **配置**：写清在设计器哪一块、哪个入口、选什么；需要 `type` 时与调色板中文名一起写。
- **条件与期望**：说明怎么配、不配是否可行；期望写用户能看见的结果。
- **计划里的任务**：可以说改哪个文件，但「做什么 / 怎样算做完」仍用界面语言。
- 改旧文档时遇到规格黑话，顺手改成界面语言。

```text
# ❌ 过滤条件不引用「订单明细」列；当前行为空
# ✅ 确认物料没配「过滤条件 = 订单明细.某列」（不配即可）→ 该行物料还空 → …

# ❌ 子字段 optionSource=linkage 且 isLinkageConfigured 时再查 sourceFormId
# ✅ 某列取值来源选「数据联动」并保存后，该行填了条件列，目标列带出源表的值
```

## 文档同步

- Codex 规则维护在根目录 `AGENTS.md`。
- Cursor 规则维护在 `.cursor/rules/*.mdc`。
- 两套规则的元数据格式可以不同，但规则含义必须保持一致。
- 具体功能设计、实施计划和待办事项维护在 `docs/`，不将规则散落到功能文档中。
- 规则发生变化时，在同一次修改中更新对应的 Codex 和 Cursor 文档，并在提交信息中说明同步范围。
