# 待办

本期明确不做，后续再做：

- [ ] 邮箱验证码（注册或敏感操作的二次确认）
- [ ] 找回密码
- [ ] 微信扫码登录
- [ ] 支付宝扫码登录
- [ ] 替换 `revoked_token` 登出作废方案

当前退出把 access JWT 哈希写入 `revoked_token`，校验时查表。弊端：每次退出插一行，表按「token 剩余有效期内的退出次数」增长；若不清理到期行会一直堆。这是在用数据库模拟 JWT 本不该持久化的黑名单。

后续按短 access + 可撤销 refresh（一行一会话，退出即删）；全端下线用 `user.tokenVersion`；紧急作废再用短 TTL 的 Redis 拒绝名单。现阶段可先改成 `tokenVersion`，去掉这张表。
