# 待办

按优先级排列。原 `front/README.md` 长清单已迁入本文件（2026-09-04），该 README 现仅保留运行说明与文档索引。

对照：`front/src/components/form-design/fieldTypes.js`、`docs/testcases/README.md`、各 `docs/superpowers/specs/`。

---

## P1 — 调色板已有、控件未完成（优先补）

- [x] **关联数据 `relate`**  
  现状：设计器/填报为禁用空下拉，无选表、选记录、属性面板。  
  目标：
  - 选主表（本应用已保存表单，**含当前表单自己**），弹窗 table **单选**一条；
  - 配置弹窗展示列、过滤条件、快捷搜索、分页（可复用选择数据交互，语义分开）；
  - 必配 **显示字段**（收起态、列表列标题）；可选「显示在表单中的字段」卡片预览（不入库）、填充到本表已有字段（拷贝值，不跟源表改值）；
  - 入库 `data[key]` = 源记录 id；列表/详情/填报按 id 再查，**源表改显示字段后刷新即跟进**；
  - 详情只读可点开跳源记录；**关联本表**时排除当前编辑中的记录（测树形层级）；
  - 画布未配主表提示「请选择主表」，已配则 preview 占位、不发查询。  
  规格：`docs/superpowers/specs/2026-09-04-relate-data-and-relate-subform-design.md`（旧稿 `2026-08-27-relate-data-design.md` 已并入）。  
  **不要**把下拉「其他表数据」或选择数据 `data` 改成存 id。

- [x] **关联子表单 `relate-subform`**  
  调色板已注册，画布/填报/属性均无实现。  
  语义（2026-09-04 定稿，与旧描述不同）：装在**被关联的主表**上的**只读表格**——查看主表某条数据详情时，列出「哪些表的哪些数据用关联数据指向了我」，可翻页。拖入时只能**从已有关联表选择**（候选 = 有关联数据字段且数据源是当前表单的表单）。本期不做可新增/可编辑/可删除开关，改数据去子表自己的页面。不入库、不进列表列、不进导入导出；不允许拖进子表单、不允许嵌套。  
  规格：`docs/superpowers/specs/2026-09-04-relate-data-and-relate-subform-design.md`。

- [x] 应用要可以改名字
- [x] 关联子表标题左侧要有icon提示，在画布中，在详情中，都需要
- [x] 关联子表里点击可以查看子表数据的详情，详情里要准备回显对应数据。
- [x] 关联子表占据整行宽度，没有字段宽度设置
- [x] 关联子表不需要设置[每页条数],而是在查看时可以自由选择，修改后重新查子表的数据。pagesize使用项目通用配置。
- [x] 主表在新增时，不需要出现关联的子表占位。只有在查看详情和编辑时才出现关联子表，显示关联子表的数据
- [ ] 当前实现下：【关联数据】，表关联自己时，新建表，还没保存，此时数据源里是选不出自己的，要先保存一下，然后才能选到自己。**也能用**。但最好是添加一个当前表的选项，这样在保存时，一块创建关系
---

## P2 — 已有组件的能力延伸（高价值）

### 选择数据 / 关联

- [ ] **选择数据：填报弹窗「允许新增数据源表数据」**  
  主表、子表均未做；规格登记为不做。

- [ ] **选择数据：主表一次多选、清空子表后整表灌入**（简道云 9024-3）  
  与子表内「未绑本行过滤时可多选加行」不同；子表用例 **X-05** 登记为另立项。

- [x] **关联数据：主表详情自动生成「关联标签页」由人在主表画布里显式加「关联子表单 `relate-subform`」**（见 P1），自动生成不做。

### 数据管理（`FormRecordList` / `FormRecordManage`）

- [ ] **导出**  
  导入、下载导入模版、表单发布里开关已有；**导出 Excel 暂不做**。

- [ ] **全功能筛选**  
  表格右侧入口 → 弹框配多条件筛选 → 持久化（建议 localStorage，与排序/便捷筛选一致）→ 支持快速清空。  
  与现有 **便捷筛选**（顶栏关键字）并存；便捷筛选目前仅支持：`input`、`textarea`、`number`、`select`、`select-multiple`、`radio`、`checkbox`（见 `quickSearch.js`）。

- [ ] **扩展便捷筛选字段类型**  
  日期/时间/人员/部门/地址等尚未进入便捷筛选；需定义查询语义后再加。

- [ ] **表底统计行**（`FormRecordList` 内 TODO 注释）

- [ ] **子表列多级表头**  
  现状：子表在列表里合并为一列摘要（` / `、`；` 拼接）。可选：按子字段展开为多级表头。

- [ ] **单元格内联编辑**  
  已支持：单行/多行文本、数字、单选/复选、下拉单选/多选（**不含**取值来源为数据联动的选项字段）、日期/时间/日期时间、地址。  
  未支持：选择数据、关联数据、人员、部门、图片、文件、流水号、子表等；上述类型及数据联动来源的选项字段仍须抽屉编辑。

### 设计器 / 发布

- [x] **工作台 Tab 顺序存本地**  
  「添加数据 / 数据管理」顺序只存本机浏览器缓存，默认添加数据在前。表单发布页已说明只在当前电脑生效。

---

## P3 — 调色板尚未有的字段类型

（子表单规格 §3.3）

- [ ] **手写签名**
- [ ] ~~**定位**~~【不做】
- [ ] **查询字段**（表单内查询控件，非列表筛选）
- [ ] ~~**富文本**~~【不做】
- [ ] **计算 / 公式字段**（展示用计算，非仅提交校验）
- [ ] **按钮**
- [ ] ~~**手机**~~【不做】
- [ ] **OCR**【不做】

---

## P4 — 表单级规则与校验

- [ ] **显隐规则**（主表、子表、标签页内字段；规格登记主表也还没有）
- [ ] **公式配置校验**（与各字段属性校验配合，覆盖多数场景）

---

## P5 — 子表单 / 工作台：另立项或低优先级

规格/用例登记「本期不做」或见到算失败：

- [ ] 子表 **Excel 导入**、**快速填报**、**Excel 粘贴加行**
- [ ] 列表按 **子表列筛选 / 排序**（主表列筛选见 P2）
- [ ] 子表 **权限组**、**嵌套子表**、**扫码加行**
- [ ] 子表 **公式 / 行汇总 / 金额汇总**
- [ ] **移动端**子表样式
- [ ] **流水号 `serialNumber` 进子表**（主表已有，子表禁止拖入）
- [ ] 子表列 **拖拽排序**（现为属性面板上移/下移）
- [ ] **整张表单说明**在新增/编辑抽屉顶部展示
- [ ] **流程表单**  
  规格：`docs/superpowers/specs/2026-09-05-workflow-form-design.md`（画布连线 + 分支，自研引擎，不引入 BPM）。  
  待办入口：项目首页跨应用；应用内左侧在应用名下、搜索/新建上再有一份（只查本应用）。同一套卡片和抽屉。没有应用权限的人只走首页。  
  表单类型：建表时选「普通表单 / 流程表单」，之后只能普通转流程（已有数据一次性记为已通过）；流程表单不能转回普通。流程表单没发布流程前不能填报，服务端也不写库。发布成功即启用。已通过后只有原发起人能在数据管理里改，改完用当前已发布版本重新审批。
- [ ] **AI 数据分析模块**

---

## P6 — 打印 / 多端（远期）

- [ ] **小票标签**设计、打印、模板库
- [ ] **大票**（含表格/详情）设计、打印
- [ ] **打印机连接**（如 Electron 中转）
- [ ] ~~**App / H5 / 小程序**~~

---

## P7 — 平台、权限与基础设施

- [ ] **普通用户首次登录强制改密码**（不改密码不可用其他功能）
- [ ] **短 access + 长 refresh、无感刷新**（与下方登出方案一并规划）
- [ ] **主键改雪花 id**（现 MySQL 自增，易被猜规律）
- [ ] **数据权限**（行级/范围）  
  规格：`docs/superpowers/specs/2026-09-05-permission-design.md` 第 3 期。先做全部 / 本人创建 / 本部门，不做自定义条件。
- [ ] **菜单（模块）权限**  
  不单独配。首页有没有应用、能不能进设计/后台，由所有者、配置名单、使用范围推出来（同上规格 §3）。
- [ ] **应用级、表单级**访问与操作权限  
  规格同上：第 2 期做配置名单（落到人）、开放使用范围（部门/角色/人员，页面叫「使用范围」不叫「发布」）、移交所有者（移交后名单里不留新旧所有者，使用范围补旧所有者为普通使用人；所有者停用后由系统管理员在管理后台仅做移交）；第 3 期做每张表「谁可以」。
- [ ] **Canvas Table**：大数据量、**分组展示**
- [ ] **视图设置**：普通翻页表格视图 vs 大量数据分组视图（依赖 Canvas Table）
- [ ] **文件预览**（Word、Excel 等）与 **在线编辑**
- [ ] **协同编辑**（如 yjs）
- [ ] **单条数据变更日志**（谁创建/修改、时间、改了哪些字段）
- [ ] **前端发版后提示用户整页刷新**
- [ ] **表单设计多人同时编辑**提示或锁（待产品定）
- [ ] 项目里有很多本地缓存的数据，是不是应该存入indexdb？尤其是表单数据管理的表格配置。再来个导出配置功能，把表格的配置带到别的地方

---

## P8 — 质量与文档

- [ ] **按组件批量生成/维护手工测试用例**（部分组件已有专项，可系统化补全）

---

## 已知产品差异（待决策，非必做）

**部门多选 + 可选范围 + 数据联动**

- 场景：联动写入的部门 id 已不在「可选范围」内。
- **简道云**：表单仍显示、可直接保存；打开弹框时上方已选区仍含该部门，下方树看不到。
- **本项目**：联动带入仍可展示并保存；**一旦打开弹框再保存**，已选区会丢掉不可见部门，外层展示也会没掉。
- 若要对齐简道云，需改 `FormDeptSelect` 已选区与提交逻辑。

---

## 认证与账号

本期明确不做，后续再做：

- [ ] 邮箱验证码（注册或敏感操作二次确认）
- [ ] 找回密码
- [ ] 微信扫码登录
- [ ] 支付宝扫码登录
- [ ] 替换 **`revoked_token` 登出作废**方案

当前退出把 access JWT 哈希写入 `revoked_token`，校验时查表。弊端：每次退出插一行，表持续增长；用 DB 模拟 JWT 黑名单。

建议：短 access + 可撤销 refresh（一行一会话）；全端下线用 `user.tokenVersion`；紧急作废用短 TTL Redis 拒绝名单；或现阶段先 `tokenVersion` 去表。


## =================================
[ ] 查看本期计划，有没有转办功能。如果没有，这要补充上，需要这个转办的功能
