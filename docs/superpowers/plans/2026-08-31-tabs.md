# 标签页组件 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. 在 `master` 上改，不开分支。未经使用者要求不 commit。

**Goal:** 做成主表「标签页」布局组件：一张表一个、默认两个 pane、内部字段仍是主表扁平数据；设计器可管理 pane 并往当前活动 tab 加字段；填报/详情按页签展示；列表与入库把内部字段当普通列。

**Architecture:** schema 在根 `fields[]` 里放一个 `type: 'tabs'`，其 `panes[].fields` 挂内部字段。读写值、校验、列表、联动一律先 `flattenFields`（只展开 pane，不展开 `subform.fields`）。标签页自己不入库。设计器活动 tab、填报当前 pane 都只存在会话里。

**Tech Stack:** Vue 3、Element Plus `el-tabs`、前端 `node:test`、后端 Jest。

**Spec:** `docs/superpowers/specs/2026-08-31-tabs-design.md`

## Global Constraints

- 开发在 `master`，不开分支、不建 worktree。
- 模板不写行内 JS；新 `el-dialog` 加 `draggable`；布局优先 flex；相邻 `el-button` 不加 `gap`。
- 图标必须从 `@element-plus/icons-vue` 确认导出后再 import。`tabs` 已用库内 `Document`，不要改名。
- 不删不改使用者已有注释和 `console.log`。
- 不勾 `front/README.md`。
- 不自动 git commit。
- 不新增 npm 依赖。
- 本期不实现子表单；允许把 `subform` 放进 pane。`flattenFields` 不得展开 `subform.fields`。
- 不支持多个标签页、套娃 tab、跨 tab / 内外拖字段、复制标签页。
- `fieldTypes.js` 已有 `{ type: 'tabs', label: '标签页', icon: Document, component: 'Tabs' }`，不要再加一条。

## File Structure

```text
front/src/components/form-design/tabsField.js                 # 新建：flatten / pane 规则 / 查找
front/src/components/form-design/tabsField.spec.js            # 新建
front/src/components/form-fill/fillValues.js                  # SKIP tabs；empty/clone/build/validate 先 flatten
front/src/components/form-fill/fillValues.spec.js
front/src/components/form-design/dataSelect.js                # fillInfluencerTips 先 flatten
front/src/components/FormDesignPanel.vue                      # 投放目标、单例拦截、嵌套增删改序
front/src/components/form-design/FormDesignCanvas.vue         # 把 pane 上下文传下去
front/src/components/form-design/FormDesignCanvasField.vue    # 画布页签 + 当前 pane 字段
front/src/components/form-design/FormDesignProps.vue          # pane 列表 CRUD；formFields 先 flatten
front/src/components/form-fill/FormFillGrid.vue               # 填报页签 + revealField
front/src/components/form-workspace/FormRecordCreateTab.vue   # 透传 revealField
front/src/components/form-workspace/FormRecordCreateDrawer.vue
front/src/components/form-workspace/FormRecordDetailDrawer.vue
front/src/components/form-workspace/FormRecordManage.vue
front/src/components/AppWorkspaceMain.vue                     # 校验失败切 pane；dictCodes flatten
front/src/components/form-workspace/FormRecordList.vue        # 列用 flatten
front/src/components/form-workspace/FormRecordQuickSearch.vue
server/src/application/form-record/flatten-fields.ts          # 新建：与前端同语义
server/src/application/form-record/flatten-fields.spec.ts
server/src/application/form-record/form-record.types.ts       # panes 可选
server/src/application/form-record/form-record.coerce.ts
server/src/application/form-record/form-record.coerce.spec.ts
server/src/application/form-record/form-record.indexes.ts
server/src/application/form-record/form-record.indexes.spec.ts
server/src/application/form-record/form-record.import.ts
server/src/application/form-record/form-record.service.ts     # unique / 导入循环 flatten
server/src/application/application.service.ts                 # listFormFields 先 flatten
server/src/application/application.service.spec.ts
```

职责：`tabsField.js` / `flatten-fields.ts` 不碰 Vue。画布只负责展示和投放；pane 增删改标题在属性面板。填报只在 `FormFillGrid` 画页签，避免 `FormFillField` 再套一层 `FormFillGrid`（联动必须看摊平后的全量字段）。

---

### Task 1: 标签页纯函数

**Files:**
- Create: `front/src/components/form-design/tabsField.js`
- Test: `front/src/components/form-design/tabsField.spec.js`

**Interfaces:**
- Produces: `MIN_TAB_PANES`（2）、`MAX_TAB_PANES`（10）、`isTabsField`、`flattenFields`、`hasTabsField`、`findTabsField`、`findFieldByKey`、`paneIdOfField`、`createTabsField`、`defaultPanes`、`nextPaneTitle`、`canAddPane`、`canRemovePane`、`neighborPaneId`

- [ ] **Step 1: 写失败测试**

```js
import assert from 'node:assert/strict'
import { test } from 'node:test'
import {
  MIN_TAB_PANES,
  MAX_TAB_PANES,
  canAddPane,
  canRemovePane,
  createTabsField,
  findFieldByKey,
  flattenFields,
  hasTabsField,
  neighborPaneId,
  nextPaneTitle,
  paneIdOfField,
} from './tabsField.js'

test('flattenFields walks panes in order and skips the tabs field itself', () => {
  const fields = [
    { key: 'before', type: 'input' },
    {
      type: 'tabs',
      key: 'tabs_1',
      panes: [
        { id: 'p1', title: 'A', fields: [{ key: 'a', type: 'input' }] },
        { id: 'p2', title: 'B', fields: [{ key: 'b', type: 'number' }] },
      ],
    },
    { key: 'after', type: 'input' },
  ]
  assert.deepEqual(
    flattenFields(fields).map((item) => item.key),
    ['before', 'a', 'b', 'after'],
  )
})

test('flattenFields does not expand subform.fields', () => {
  const fields = [
    {
      type: 'tabs',
      key: 'tabs_1',
      panes: [
        {
          id: 'p1',
          title: 'A',
          fields: [
            {
              key: 'kids',
              type: 'subform',
              fields: [{ key: 'col', type: 'input' }],
            },
          ],
        },
      ],
    },
  ]
  assert.deepEqual(
    flattenFields(fields).map((item) => item.key),
    ['kids'],
  )
})

test('hasTabsField and createTabsField default two panes', () => {
  const field = createTabsField('tabs_1', ['id1', 'id2'])
  assert.equal(field.type, 'tabs')
  assert.equal(field.width, '1')
  assert.equal(field.panes.length, MIN_TAB_PANES)
  assert.equal(field.panes[0].title, '标签页1')
  assert.equal(field.panes[1].title, '标签页2')
  assert.equal(hasTabsField([field]), true)
  assert.equal(hasTabsField([{ key: 'n', type: 'input' }]), false)
})

test('nextPaneTitle uses max 标签页N then count+1', () => {
  assert.equal(
    nextPaneTitle([{ title: '标签页1' }, { title: '标签页2' }]),
    '标签页3',
  )
  assert.equal(
    nextPaneTitle([{ title: '基本信息' }, { title: '联系' }]),
    '标签页3',
  )
  assert.equal(nextPaneTitle([{ title: '标签页5' }]), '标签页6')
})

test('pane guards and neighbor', () => {
  const two = [{ id: 'a' }, { id: 'b' }]
  assert.equal(canRemovePane(two), false)
  assert.equal(canAddPane(two), true)
  const ten = Array.from({ length: MAX_TAB_PANES }, (_, i) => ({ id: String(i) }))
  assert.equal(canAddPane(ten), false)
  assert.equal(neighborPaneId([{ id: 'a' }, { id: 'b' }, { id: 'c' }], 'b'), 'c')
  assert.equal(neighborPaneId([{ id: 'a' }, { id: 'b' }], 'b'), 'a')
})

test('findFieldByKey and paneIdOfField look inside panes', () => {
  const fields = [
    { key: 'out', type: 'input' },
    {
      type: 'tabs',
      key: 'tabs_1',
      panes: [
        { id: 'p1', title: 'A', fields: [{ key: 'in', type: 'input' }] },
        { id: 'p2', title: 'B', fields: [] },
      ],
    },
  ]
  assert.equal(findFieldByKey(fields, 'in')?.type, 'input')
  assert.equal(paneIdOfField(fields, 'in'), 'p1')
  assert.equal(paneIdOfField(fields, 'out'), '')
  assert.equal(paneIdOfField(fields, 'tabs_1'), '')
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node --test front/src/components/form-design/tabsField.spec.js`

Expected: FAIL，`tabsField.js` 不存在或导出未定义。

- [ ] **Step 3: 最小实现**

`front/src/components/form-design/tabsField.js`：

```js
export const MIN_TAB_PANES = 2
export const MAX_TAB_PANES = 10

export function isTabsField(field) {
  return field?.type === 'tabs'
}

export function flattenFields(fields) {
  const out = []
  for (const field of fields || []) {
    if (isTabsField(field)) {
      for (const pane of field.panes || []) {
        out.push(...flattenFields(pane.fields || []))
      }
      continue
    }
    out.push(field)
  }
  return out
}

export function hasTabsField(fields) {
  return (fields || []).some(isTabsField)
}

export function findTabsField(fields) {
  return (fields || []).find(isTabsField) || null
}

export function findFieldByKey(fields, key) {
  for (const field of fields || []) {
    if (field.key === key) return field
    if (isTabsField(field)) {
      for (const pane of field.panes || []) {
        const found = findFieldByKey(pane.fields || [], key)
        if (found) return found
      }
    }
  }
  return null
}

export function paneIdOfField(fields, key) {
  const tabs = findTabsField(fields)
  if (!tabs) return ''
  for (const pane of tabs.panes || []) {
    if ((pane.fields || []).some((item) => item.key === key)) return pane.id
  }
  return ''
}

export function defaultPanes(ids) {
  return [
    { id: ids[0], title: '标签页1', fields: [] },
    { id: ids[1], title: '标签页2', fields: [] },
  ]
}

export function createTabsField(key, paneIds) {
  return {
    type: 'tabs',
    key,
    component: 'Tabs',
    title: '标签页',
    placeholder: '',
    width: '1',
    required: false,
    disabled: false,
    editable: true,
    description: '',
    panes: defaultPanes(paneIds),
  }
}

export function nextPaneTitle(panes) {
  let max = 0
  let hasNumbered = false
  for (const pane of panes || []) {
    const match = String(pane.title || '').match(/^标签页(\d+)$/)
    if (!match) continue
    hasNumbered = true
    max = Math.max(max, Number(match[1]))
  }
  const n = hasNumbered ? max + 1 : (panes?.length || 0) + 1
  return `标签页${n}`
}

export function canAddPane(panes) {
  return (panes?.length || 0) < MAX_TAB_PANES
}

export function canRemovePane(panes) {
  return (panes?.length || 0) > MIN_TAB_PANES
}

export function neighborPaneId(panes, removedId) {
  const list = panes || []
  const index = list.findIndex((pane) => pane.id === removedId)
  if (index < 0) return list[0]?.id || ''
  return list[index + 1]?.id || list[index - 1]?.id || ''
}
```

- [ ] **Step 4: 再跑测试**

Run: `node --test front/src/components/form-design/tabsField.spec.js`

Expected: PASS

- [ ] **Step 5: Commit（仅当使用者要求）**

```bash
git add front/src/components/form-design/tabsField.js front/src/components/form-design/tabsField.spec.js
git commit -m "新增标签页 schema 纯函数：摊平 panes 且不展开子表列"
```

---

### Task 2: 填报值与提示走 flatten

**Files:**
- Modify: `front/src/components/form-fill/fillValues.js`
- Modify: `front/src/components/form-fill/fillValues.spec.js`
- Modify: `front/src/components/form-design/dataSelect.js`（`fillInfluencerTips` 入参先 flatten）

**Interfaces:**
- Consumes: `flattenFields`（Task 1）
- Produces: `isFillable` 对 `tabs` 为 false；`emptyRecordValues` / `cloneRecordValues` / `buildRecordData` / `validateRequired` 使用摊平后的字段；`firstRequiredError(fields, values) => { message, key } | null`

- [ ] **Step 1: 在 fillValues.spec.js 追加失败测试**

```js
import { flattenFields } from '../form-design/tabsField.js'
import { firstRequiredError, isFillable } from './fillValues.js'

test('tabs is layout-only and inner fields persist', () => {
  const tabs = {
    type: 'tabs',
    key: 'tabs_1',
    panes: [
      { id: 'p1', title: 'A', fields: [{ key: 'name', type: 'input', title: '姓名', required: true }] },
    ],
  }
  assert.equal(isFillable(tabs), false)
  assert.deepEqual(emptyRecordValues([tabs]), { name: undefined })
  assert.deepEqual(buildRecordData([tabs], { name: '张三' }), { name: '张三' })
  assert.equal(validateRequired([tabs], { name: '' }), '请填写「姓名」')
  assert.deepEqual(firstRequiredError([tabs], { name: '' }), {
    message: '请填写「姓名」',
    key: 'name',
  })
})
```

`emptyRecordValues` 等现有测试仍应通过：内部改为先 `flattenFields` 再循环。无 tabs 时 flatten 等于原数组。

- [ ] **Step 2: 跑测试确认失败**

Run: `node --test front/src/components/form-fill/fillValues.spec.js`

Expected: FAIL（`firstRequiredError` 未导出，或 tabs 被当成可填字段 / 内部字段未进 payload）。

- [ ] **Step 3: 最小实现**

`fillValues.js`：

- `SKIP_TYPES` 增加 `'tabs'`。
- 顶部 `import { flattenFields } from '../form-design/tabsField.js'`。
- `emptyRecordValues` / `cloneRecordValues` / `buildRecordData` / `validateRequired` 开头：`fields = flattenFields(fields)`。
- 抽出：

```js
export function firstRequiredError(fields, values) {
  for (const field of flattenFields(fields)) {
    if (!isFillable(field) || !field.required) continue
    const missing =
      field.type === 'address'
        ? !isAddressValueReady(field, values[field.key])
        : isEmptyValue(field, values[field.key])
    if (missing) {
      return {
        message: `请填写「${field.title || '未命名'}」`,
        key: field.key,
      }
    }
  }
  return null
}

export function validateRequired(fields, values) {
  return firstRequiredError(fields, values)?.message || ''
}
```

`dataSelect.js` 的 `fillInfluencerTips`：循环前 `fields = flattenFields(fields)`，并 import `flattenFields`。这样画布提示能看到 pane 里「选择数据」的填充目标。

- [ ] **Step 4: 再跑测试**

Run: `node --test front/src/components/form-fill/fillValues.spec.js`

Expected: PASS

- [ ] **Step 5: Commit（仅当使用者要求）**

```bash
git add front/src/components/form-fill/fillValues.js front/src/components/form-fill/fillValues.spec.js front/src/components/form-design/dataSelect.js
git commit -m "填报取值与必填改为摊平标签页内部字段"
```

---

### Task 3: 后端 flatten 与入库

**Files:**
- Create: `server/src/application/form-record/flatten-fields.ts`
- Create: `server/src/application/form-record/flatten-fields.spec.ts`
- Modify: `server/src/application/form-record/form-record.types.ts`
- Modify: `server/src/application/form-record/form-record.coerce.ts`
- Modify: `server/src/application/form-record/form-record.coerce.spec.ts`
- Modify: `server/src/application/form-record/form-record.indexes.ts`
- Modify: `server/src/application/form-record/form-record.indexes.spec.ts`
- Modify: `server/src/application/form-record/form-record.import.ts`
- Modify: `server/src/application/form-record/form-record.service.ts`（unique 与导入里按字段循环处）
- Modify: `server/src/application/application.service.ts`（`listFormFields` → `toOptionFields` 前 flatten）
- Modify: `server/src/application/application.service.spec.ts`

**Interfaces:**
- Produces: `flattenFields(fields: FormField[] | null | undefined): FormField[]`，语义与前端一致（展开 `tabs.panes[].fields`，不展开 `subform`）

- [ ] **Step 1: 写 flatten 失败测试**

`flatten-fields.spec.ts`：与 Task 1 前两个用例相同（顺序 + 不展开 subform）。

`form-record.coerce.spec.ts` 追加：

```ts
it('coerces fields inside tabs and drops the tabs key', () => {
  const nested = [
    { key: 'before', type: 'input' },
    {
      key: 'tabs_1',
      type: 'tabs',
      panes: [
        {
          id: 'p1',
          title: 'A',
          fields: [{ key: 'name', type: 'input' }],
        },
      ],
    },
  ];
  expect(
    coerceRecordData(nested, {
      before: 'x',
      tabs_1: { no: true },
      name: '张三',
    }),
  ).toEqual({ before: 'x', name: '张三' });
});
```

`form-record.indexes.spec.ts`：在现有用例上增加 tabs 包裹的 `input`，期望出现 `idx_data_` 内部 key，不出现 tabs 自己。

`application.service.spec.ts` 的 `listFormFields`：给某张表加 `tabs` + pane 内 `input`，期望返回该 input，不返回 tabs。

- [ ] **Step 2: 跑测试确认失败**

Run: `cd server; npx jest src/application/form-record/flatten-fields.spec.ts src/application/form-record/form-record.coerce.spec.ts src/application/form-record/form-record.indexes.spec.ts src/application/application.service.spec.ts --testPathPattern="listFormFields|flatten|coerce|indexes"`

更直接：

```
cd server
npx jest src/application/form-record/flatten-fields.spec.ts src/application/form-record/form-record.coerce.spec.ts src/application/form-record/form-record.indexes.spec.ts
```

Expected: FAIL（新文件不存在或 coerce 仍按一层扫，`tabs_1` 可能进 default 分支留下脏值）。

- [ ] **Step 3: 最小实现**

`form-record.types.ts` 给 `FormField` 增加可选：

```ts
panes?: { id: string; title?: string; fields?: FormField[] }[];
```

`flatten-fields.ts`：与前端 `flattenFields` 同逻辑（`field.type === 'tabs'` 时展开 panes）。

`coerceFieldValue` 的 `divider` 分支加上 `case 'tabs':`，返回 `undefined`。`coerceRecordData` / `mergeRecordData` 循环改为 `for (const field of flattenFields(fields ?? []))`。

`targetDataIndexNames`：先 `flattenFields(fields)` 再按 `FILTERABLE_TYPES` 过滤。不要把 `tabs` 加入 FILTERABLE。

`importableFields`：先 flatten，再过滤；`IMPORT_SKIP_TYPES` 加上 `'tabs'`（防御未 flatten 的调用）。

`form-record.service.ts`：导入循环（约 250 行）和 `assertUniqueFields`（约 288 行）改为 `for (const field of flattenFields(fields ?? []))`。**保留** `assertUniqueFields` 循环内已有注释 `// 目前进对[单行文本]进行重复值检测`，只改遍历来源。字典 code 收集同样走 flatten。

`application.service.ts` `listFormFields`：`this.toOptionFields(flattenFields(parseFormSchema(form.fields).fields))`。不要把 `tabs` 加入 `OPTION_FIELD_TYPES`。

- [ ] **Step 4: 再跑测试**

```
cd server
npx jest src/application/form-record/flatten-fields.spec.ts src/application/form-record/form-record.coerce.spec.ts src/application/form-record/form-record.indexes.spec.ts src/application/form-record/form-record.import.spec.ts src/application/form-record/form-record.service.spec.ts src/application/application.service.spec.ts
```

Expected: PASS

- [ ] **Step 5: Commit（仅当使用者要求）**

```bash
git add server/src/application/form-record/flatten-fields.ts server/src/application/form-record/flatten-fields.spec.ts server/src/application/form-record/form-record.types.ts server/src/application/form-record/form-record.coerce.ts server/src/application/form-record/form-record.coerce.spec.ts server/src/application/form-record/form-record.indexes.ts server/src/application/form-record/form-record.indexes.spec.ts server/src/application/form-record/form-record.import.ts server/src/application/form-record/form-record.service.ts server/src/application/application.service.ts server/src/application/application.service.spec.ts
git commit -m "入库、索引、导入与他表字段列表按标签页内部字段摊平"
```

---

### Task 4: 设计器画布与投放

**Files:**
- Modify: `front/src/components/FormDesignPanel.vue`
- Modify: `front/src/components/form-design/FormDesignCanvas.vue`
- Modify: `front/src/components/form-design/FormDesignCanvasField.vue`

**Interfaces:**
- Consumes: Task 1 全部导出
- Produces: `activePaneId`（组件内 ref，不进 schema）；`addField(item, beforeKey, paneId)`；根上拖排序与 pane 内拖排序分开；第二个 `tabs` 被拦截

- [ ] **Step 1: 改 selectedField / dictCodes / 增删改序**

`FormDesignPanel.vue`：

```js
import {
  createTabsField,
  findFieldByKey,
  flattenFields,
  hasTabsField,
  isTabsField,
  paneIdOfField,
} from './form-design/tabsField.js'
```

- `selectedField`：`findFieldByKey(fields.value, selectedKey.value)`。
- `dictCodes`：遍历 `flattenFields(fields.value)`。
- `const activePaneId = ref('')`
- `addField(item, beforeKey, paneId)`：
  1. 若 `item.type === 'tabs'` 且 `hasTabsField(fields.value)`：`ElMessage.warning('每个表单只能有一个标签页')` 然后 return。
  2. 若 `item.type === 'tabs'`：`field = createTabsField(nextKey(), [nextKey(), nextKey()])`，插入**根**列表（`beforeKey` 只在根上找），`activePaneId = field.panes[0].id`，`selectField(field)`，return。
  3. 其它类型先按现有逻辑组 `field`；`width` 对 divider 仍为 `'1'`。
  4. 目标 pane：参数 `paneId`，否则若当前选中是 tabs 或 `paneIdOfField(fields, selectedKey)` 有值，用 `activePaneId`（若空则 tabs 的第一个 pane）。
  5. 有目标 pane：在该 `pane.fields` 里按 `beforeKey` 插入（找不到则 push）；否则在根 `fields` 按现有 `beforeKey` 插入。
  6. `selectField(field)`；若进了 pane，确保 `activePaneId` 为该 pane。
- `removeField`：从根或某个 `pane.fields` 里按 `key` 滤掉（不要只 `fields.value.filter`）。删的是整个 tabs 时，现有确认框即可，确认后根上去掉该组件。
- `reorderFields(fromKey, toKey)`：若两 key 都在同一 `pane.fields`，只在该数组 splice；若都在根上，只在根上 splice；跨集合则 **no-op**（禁止拖出/拖进/跨 tab）。
- `selectField`：若选中的是 tabs，不要清空 `activePaneId`（没有则设第一 pane）；若选中内部字段，`activePaneId = paneIdOfField(...)`。
- 画布 `@add="addField"` 改为面板函数 `onCanvasAdd(item, beforeKey, paneId)`，内部调 `addField`。
- 模板不写行内 JS。

- [ ] **Step 2: Canvas 增加 pane 事件**

`FormDesignCanvas.vue`：props 增加 `activePaneId`（String）。emits 增加 `update:activePaneId`。`add` 改为 `(item, beforeKey, paneId)`。

根上 `onDrop` / `onDropCanvas`：palette 落到**根字段或画布空白**时 `paneId` 不传（根）。把 `activePaneId` 传给 `FormDesignCanvasField`。

`fillTips` 已因 Task 2 在 `fillInfluencerTips` 内 flatten，这里可保持 `fillInfluencerTips(props.fields)`。

- [ ] **Step 3: CanvasField 画页签**

当 `field.type === 'tabs'`（在现有 `subform` 占位之前）：

- 外层仍是 `.canvas-field`，`width` 用 `'1'`。整块可在根上拖（现有 draggable）。
- 页签条：flex，`v-for="pane in field.panes"`。点页签：`emit('select')` 且 `emit('update:activePaneId', pane.id)`。不要在模板里写赋值语句，抽 `onPaneClick(pane)`。
- 内容区：当前 `activePaneId` 对应 pane（找不到则第一 pane）。`@dragover.prevent`、`@drop.prevent.stop` → 若是 palette 则 `emit('add', item, undefined, pane.id)`；若是已有字段 key 则 `emit('reorder', fromKey, 当前 pane 内目标 key 或空)`。点空白内容区：`emit('select')` 选中标签页组件。
- 内部字段：递归 `FormDesignCanvasField`。内部 `@dragstart.stop`，避免拖子字段时拖走整个标签页。内部 drop 只 reorder 同一 pane。
- 内部字段的 `selected` / `fill-tip` / `items` 与根字段相同来源（panel 已按 flatten 拉字典）。
- 空 pane 显示一句「从左侧选择或拖动字段添加到此标签页」。

点内部字段：`emit('select', child)` 需要 Canvas 把 select 接到 panel 的 `selectField`。现在是 `@select="$emit('select', field)"` 写死当前 field。改为 `@select="onSelect"`：子组件 `emit('select')` 无参时用自己的 field；tabs 内子字段 `emit('select', child)`。保持简单：子 `FormDesignCanvasField` 继续无参 `select`，tabs 内容里 `@select="$emit('select', child)"` **违规（行内）**，改为：

```js
function onSelectChild(child) {
  emit('select', child)
}
```

模板：`@select="onSelectChild"` 不够，因为要带 child。用：

```js
function bindSelect(child) {
  return () => emit('select', child)
}
```

不能在模板里写箭头。改为 emit 始终带 field：把 `FormDesignCanvasField` 的 select 改成 `emit('select', props.field)`，根 Canvas `@select="onSelect"` 直接 `$emit('select', field)` 仍是行内。Panel 已有 `selectField`。Canvas：

```js
function onSelect(field) {
  emit('select', field)
}
```

`FormDesignCanvasField`：`@click="onSelect"`，`function onSelect() { emit('select', props.field) }`。根：`@select="onSelect"`。Tabs 内：`@select="onSelect"` 同一函数，子组件 emit 的是 child field。**改 emit 载荷**：现在是无参，改为带 field。同步改 Canvas `@select="onSelect"`，`onSelect(field) { emit('select', field) }`。复制/删除同理已带 field。只改 select 这一处即可。

- [ ] **Step 4: 手测设计器投放**

`front` 下 `npm run dev`，打开表单设计：

1. 拖入标签页 → 默认两个页签「标签页1/2」，整行。
2. 选中标签页，点「单行文本」→ 进当前 pane。
3. 点画布空白（标签页外）再点「数字」→ 出现在标签页外。
4. 再拖一个标签页 → 提示「每个表单只能有一个标签页」。
5. pane 内两个字段互相拖，只改该 pane 顺序；拖到外面的字段上不移动。

- [ ] **Step 5: Commit（仅当使用者要求）**

```bash
git add front/src/components/FormDesignPanel.vue front/src/components/form-design/FormDesignCanvas.vue front/src/components/form-design/FormDesignCanvasField.vue
git commit -m "设计器支持标签页画布展示及向当前活动 tab 添加字段"
```

---

### Task 5: 字段属性管理 pane

**Files:**
- Modify: `front/src/components/form-design/FormDesignProps.vue`

**Interfaces:**
- Consumes: `nextPaneTitle`、`canAddPane`、`canRemovePane`、`neighborPaneId`、`MIN_TAB_PANES`、`MAX_TAB_PANES`
- Produces: 选中 `tabs` 时只编辑 `field.panes`（改标题、添加、删除确认、拖排序）；`emit('active-pane', id)` 以便删除当前 pane 后由 panel 更新 `activePaneId`

更简单：删除/添加直接改 `field.panes`（现有属性面板就是 mutate `field`）。活动 pane 由 panel 根据 `activePaneId` 是否仍存在来纠正。Panel `watch` selected tabs 的 panes：若 `activePaneId` 不在列表里，设为第一 pane。不必新 emit。

- [ ] **Step 1: 选中 tabs 时隐藏填报属性**

在「字段标题」和后面那大段 `v-if="field.type !== 'divider'"` 外包一层：`field.type !== 'tabs'` 时才显示标题/必填/宽度等。`tabs` 不显示字段标题、占位、说明、必填、宽度。

顶部「组件类型」仍显示「标签页」。

- [ ] **Step 2: pane 列表 UI**

选中 `tabs` 时增加一块（flex 列）：

- 标题「标签页」。
- 每行：拖动手柄区域 + `el-input` 绑定 `pane.title`（`maxlength="32"`）。`@blur="onPaneTitleBlur(pane)"`：trim，空则还原为失焦前或「标签页」。不要在模板里写语句。
- 删除按钮：`canRemovePane(field.panes)` 为 false 时 disabled。点击 `onRemovePane(pane)`：

```js
async function onRemovePane(pane) {
  if (!canRemovePane(props.field.panes)) return
  try {
    await ElMessageBox.confirm(
      `确定删除「${pane.title || '未命名'}」？该标签页及其内部字段将一并删除。`,
      '删除标签页',
      { confirmButtonText: '删除', cancelButtonText: '取消', type: 'warning' },
    )
  } catch {
    return
  }
  props.field.panes = props.field.panes.filter((item) => item.id !== pane.id)
}
```

- 「添加标签页」按钮：`canAddPane` 为 false 时 disabled。点击：push `{ id: crypto.randomUUID(), title: nextPaneTitle(props.field.panes), fields: [] }`。相邻按钮不要给容器加 `gap`。
- 拖排序：行 `draggable`，`dragstart` 记下 pane id，`drop` 在目标行上对 `field.panes` splice，逻辑与 `reorderFields` 相同，只作用于 `panes` 数组。

确认 `@element-plus/icons-vue` 有 `Delete`（画布已用）、添加可用已有 `Plus`。不要编不存在的图标。

- [ ] **Step 3: 手测属性**

1. 改 pane 标题，画布页签文字跟着变。
2. 添加直到 10 个，按钮禁用。
3. 只剩 2 个时删除禁用。
4. 给某 pane 加字段后删除该 pane，确认框出现，确认后字段从预览 JSON 消失。
5. 拖 pane 行，画布页签顺序变。

- [ ] **Step 4: Commit（仅当使用者要求）**

```bash
git add front/src/components/form-design/FormDesignProps.vue
git commit -m "标签页属性支持添加、删除、改标题和拖动排序"
```

---

### Task 6: 填报 / 详情页签与校验切 pane

**Files:**
- Modify: `front/src/components/form-fill/FormFillGrid.vue`
- Modify: `front/src/components/form-workspace/FormRecordCreateTab.vue`
- Modify: `front/src/components/form-workspace/FormRecordCreateDrawer.vue`（若内嵌 Grid，同样 expose）
- Modify: `front/src/components/form-workspace/FormRecordDetailDrawer.vue`
- Modify: `front/src/components/form-workspace/FormRecordManage.vue`
- Modify: `front/src/components/AppWorkspaceMain.vue`

**Interfaces:**
- Consumes: `flattenFields`、`findTabsField`、`paneIdOfField`、`firstRequiredError`
- Produces: `FormFillGrid.revealField(key)`；逻辑侧（联动、他表下拉）使用 `flatFields`

- [ ] **Step 1: FormFillGrid 渲染页签**

```js
import { flattenFields, findTabsField, isTabsField, paneIdOfField } from '../form-design/tabsField.js'

const flatFields = computed(() => flattenFields(props.fields))
const tabsField = computed(() => findTabsField(props.fields))
const activePaneId = ref('')

watch(
  tabsField,
  (tabs) => {
    const ids = (tabs?.panes || []).map((pane) => pane.id)
    if (!ids.includes(activePaneId.value)) {
      activePaneId.value = ids[0] || ''
    }
  },
  { immediate: true },
)

function revealField(key) {
  const paneId = paneIdOfField(props.fields, key)
  if (paneId) activePaneId.value = paneId
}

defineExpose({ revealField })
```

原先所有 `props.fields.filter` / `props.fields.some` / 传给 `buildSourceQuery` / `hasLinkage` 遍历的，改用 `flatFields.value`。`fillTips` 可继续 `fillInfluencerTips(props.fields)`（内部已 flatten）。

模板：不要对根字段一律 `FormFillField`。`v-for="field in fields"`：

- `isTabsField(field)`：一个 `div.fill-tabs`（`grid-column: span 12`），内 `el-tabs`，`v-model` 不要行内，用 `:model-value="activePaneId"` `@tab-change="onTabChange"`。

```js
function onTabChange(name) {
  activePaneId.value = name
}
```

每个 `el-tab-pane` 的 `name` 为 `pane.id`，`label` 为 `pane.title`。pane 内再放一个 `div.fill-grid`，`FormFillField` 遍历 `pane.fields`。给内部字段传 `:form-fields="flatFields"`、同一份 `values`、`@fill="onFill"`。

- 非 tabs：现有 `FormFillField`。

`.fill-tabs`：`grid-column: span 12; min-width: 0;`。不要用 354px 限制（那是 `.fill-field` 的）。页签内容区里的字段仍用 `FormFillField` 的宽度 class。

`el-tabs` 切换不要写 `@tab-change="activePaneId = $event"`。

- [ ] **Step 2: 校验失败切 pane**

`FormRecordCreateTab.vue`：给内部 Grid 加 `ref="gridRef"`，`defineExpose({ revealField: (key) => gridRef.value?.revealField(key) })`。取消/保存若仍是模板 `$emit`，改到相关代码时抽成 `onCancel` / `onSave` 函数（本文件会改到模板，顺手抽掉行内 `$emit`）。

`AppWorkspaceMain.vue` `onSave`：

```js
const err = firstRequiredError(fields.value, values)
if (err) {
  ElMessage.warning(err.message)
  createTabRef.value?.revealField(err.key)
  return
}
```

`FormRecordManage` 新建保存、`FormRecordDetailDrawer` 保存同样：先 `firstRequiredError`，再 `gridRef.revealField`。CreateDrawer 若自己包 Grid，同样 expose。

`dictCodes`（Main / Manage）：遍历 `flattenFields(fields.value)`。

- [ ] **Step 3: 手测填报**

1. 标签页前有字段、两个 pane 各有字段、标签页后有字段：添加数据页按这个顺序出现，页签可切。
2. 只填当前 pane，另一 pane 必填为空 → 保存提示「请填写「…」」并切到该 pane。
3. 两个 pane 的字段都能保存进同一条记录；刷新详情仍按页签展示，值还在。
4. pane A 的选择数据填充 pane B 的字段：不必先打开 B，保存后 B 里有值。

- [ ] **Step 4: Commit（仅当使用者要求）**

```bash
git add front/src/components/form-fill/FormFillGrid.vue front/src/components/form-workspace/FormRecordCreateTab.vue front/src/components/form-workspace/FormRecordCreateDrawer.vue front/src/components/form-workspace/FormRecordDetailDrawer.vue front/src/components/form-workspace/FormRecordManage.vue front/src/components/AppWorkspaceMain.vue
git commit -m "填报与详情按标签页展示，校验失败时切到对应 pane"
```

---

### Task 7: 列表与选字段下拉摊平

**Files:**
- Modify: `front/src/components/form-workspace/FormRecordList.vue`
- Modify: `front/src/components/form-workspace/FormRecordQuickSearch.vue`
- Modify: `front/src/components/form-design/FormDesignProps.vue`（`formFields` 先 flatten）

**Interfaces:**
- Consumes: `flattenFields`、`isFillable`

- [ ] **Step 1: 列表列**

`FormRecordList.vue`：

```js
const tableFields = computed(() => flattenFields(props.fields).filter(isFillable))
```

不要给 tabs 单独一列。列顺序：标签页前 → pane1 → pane2 → 标签页后。

`FormRecordQuickSearch.vue`：`visibleFields` 以及 `loadQuickSearchPrefs` / `saveQuickSearchPrefs` / watch 依赖的 `props.fields`，改为 `flattenFields(props.fields)` 后再用（可 `computed(() => flattenFields(props.fields))` 一次）。

- [ ] **Step 2: 设计器「选本表字段」**

`FormDesignProps.vue`：

```js
const formFields = computed(() =>
  flattenFields(props.fields).filter(
    (item) => isFillable(item) && item.key !== props.field?.key,
  ),
)
```

他表字段树走 `listFormFieldsApi`，Task 3 摊平后即可，不必改 `FormFieldSourcePicker.vue`。`FieldSelect` 吃的是 Props 传来的 `formFields`，摊平一次即可。

- [ ] **Step 3: 手测列表与联动配置**

1. 数据管理表头出现 pane 内字段，没有「标签页」列。
2. 给 pane 内单行文本配数据联动，来源字段下拉能看到另一 pane 和标签页外的字段。
3. 选择数据填充映射的目标字段同样能选到 pane 内字段。

- [ ] **Step 4: 跑相关单测**

```
node --test front/src/components/form-design/tabsField.spec.js front/src/components/form-fill/fillValues.spec.js
cd server
npx jest src/application/form-record/flatten-fields.spec.ts src/application/form-record/form-record.coerce.spec.ts src/application/form-record/form-record.indexes.spec.ts
```

Expected: PASS

- [ ] **Step 5: Commit（仅当使用者要求）**

```bash
git add front/src/components/form-workspace/FormRecordList.vue front/src/components/form-workspace/FormRecordQuickSearch.vue front/src/components/form-design/FormDesignProps.vue
git commit -m "列表与字段下拉使用摊平后的标签页内部字段"
```

---

## 自检（对照 spec）

| Spec | Task |
|---|---|
| 视觉编组、扁平 data、flatten 不进 subform.fields | 1, 2, 3 |
| 单表一个 tabs、最少 2 / 最多 10 pane | 1, 4, 5 |
| 投放：选中 tabs/内部 → 当前 pane；选中外部/画布空白 → 根 | 4 |
| 只在当前 pane 内拖字段 | 4 `reorderFields` 跨集合 no-op |
| 属性增删改标题、拖 pane、删 pane 二次确认 | 5 |
| 画布复制不考虑 | 不改复制按钮 |
| 填报/详情页签、未打开 pane 必填、切到错误 pane | 2, 6 |
| 预览仍是 JSON | 不改预览弹窗 |
| 列表无「标签页」列 | 7 |
| 联动/选择数据能选到内部字段、允许跨 tab | 2, 6, 7 |
| 后端 coerce/索引/导入/listFormFields | 3 |
| 子表单本期不实现，合并后再验 | 1 测试夹具 + spec 第 8 节，无实现任务 |
