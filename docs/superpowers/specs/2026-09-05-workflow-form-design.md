# 流程表单 需求设计

日期：2026-09-05  
状态：待确认  
来源：`docs/TODO.md` P5「流程表单」；2026-09-04 会话里对自研审批流的讨论  
相关：

- 表单字段与填报：`docs/superpowers/specs/2026-08-23-select-table-source-design.md` 及后续各控件规格
- 填报存储：`docs/superpowers/specs/2026-08-24-mysql-mongodb-storage-design.md`
- 组织与角色：`docs/superpowers/specs/2026-08-22-admin-organization-rbac-design.md`
- 子表节点权限（只登记、本期不做）：`docs/superpowers/specs/2026-08-30-subform-design.md` §8.5

范围：给**已经能填报的表单**加上「提交后按节点一步步审批，直到结束」。自研状态机，不引入 Camunda / Flowable / Activiti。对照界面写：设计器怎么配、发起人怎么交、审批人怎么批、数据管理里看到什么。

---

## 1. 一句话说清

普通表单：人填完点保存，数据立刻就是正式数据，谁有数据管理权限就能改、能删。

**流程表单**：人填完点「提交」，数据先进入审批。当前节点的审批人通过或驳回后，才走到下一节点或结束。**没走完的数据不能当正式业务数据随便改。** 通过之后，这条数据才能被别的表的关联数据、关联子表单当作正式记录挑到。下拉单选「其他表数据」取的是字段值不是资格，和现在一样不过滤流程状态。

一张表单是不是流程表单，不看调色板有没有新控件，只看它有没有**已发布的流程**。

---

## 2. 贯穿全文的例子

准备一张 **请假单**（和现有「添加数据 / 数据管理」同一套表单，只是多绑了流程）。

| 字段标题 | 调色板控件 |
|---|---|
| 请假人 | 人员单选 `member` |
| 请假类型 | 下拉单选框 `select`（事假 / 病假） |
| 开始日期 | 日期选择 `date` |
| 结束日期 | 日期选择 `date` |
| 事由 | 多行文本 `textarea` |

流程在 **流程设计** 画布上画出来（不是一张从上到下的清单）：

```text
              开始
               │
            【分支】请假类型
           ／事假        ＼病假（否则也可走这边）
    部门审批                    │
    （角色：部门经理，或签）     │
           ＼                  ／
              人事备案（指定人员：张三、李四，会签）
               │
              结束
```

于是：

- 员工在工作台打开请假单，填完点「提交」。系统从「开始」顺着连线走：先到分支，看这条数据的「请假类型」。
- **事假**：进入「部门审批」（**或签**）。部门经理角色下的人各自有一条待办；张三、李四还没有。某位经理点「通过」后，未处理的经理待办取消，待办转到「人事备案」。
- **病假**：跳过部门审批，直接到「人事备案」。
- **人事备案**配了张三、李四且为 **会签**：两人都有一条待办。张三先通过，流程仍停在人事备案，等李四；李四也通过后才到结束。若张三驳回，李四的待办取消，整单已驳回。
- 流程走到结束，这条请假变成「已通过」。人事表如果用关联数据指向请假单，现在可以选到它。
- 若当前审批人点「驳回」并写原因：状态变成「已驳回」，发起人改内容再提交，还是**同一条数据**。再次提交时**从开始重新沿连线走**（类型若改成病假，就不再进部门审批）。

下文凡写「请假单」都指这个例子。

---

## 3. 和现有能力怎么区分

| 人在做什么 | 普通表单（现在） | 流程表单（本文） |
|---|---|---|
| 工作台「添加数据」 | 填完点保存，立刻正式入库 | 可「保存草稿」或「提交」。提交才进审批 |
| 数据管理里改一行 | 编辑抽屉或单元格内联编辑 | **审批中不能当普通数据改**；已通过后按发布页开关决定能否再改（见 §11） |
| 谁能看见这条 | 能进这张表数据管理的人 | 另外还有：发起人看自己的、当前审批人看待办。数据管理仍给能进该表的人看，但带流程状态 |
| 关联数据 / 选择数据去挑这条 | 保存后就能挑 | **只有已通过**才能被别的表当正式数据挑到（草稿、审批中、已驳回都不出现在选记录弹窗里） |

流程表单**不是**调色板新控件，也不改现有字段 `type`。请假类型、日期、人员选择怎么配，仍按各自规格。

---

## 4. 一张表单怎么变成流程表单

表单设计顶栏现在是：表单设计 / 表单发布 / 数据管理。

**新增第四个页签：流程设计**，放在「表单设计」和「表单发布」之间。能进表单设计的人就能进流程设计（和现在谁能保存字段一致，不另做一套权限）。

判定：

- 这张表**没有**已发布的流程 → 仍是普通表单，工作台、数据管理、导入导出行为不变。
- 这张表**有**已发布的流程 → 它是流程表单。工作台「添加数据」改成草稿 + 提交；新产生的数据都走流程。

**一张表同一时刻最多一个已发布流程。** 流程可以改、再发布，成为新版本（§13）。关掉「启用流程」（流程设计页一个开关）后，新提交按普通表单保存；已经在走的审批继续按**当时那个版本**走完，不中断、不改节点。

绑定流程之前就已经存在的历史数据：没有流程实例，按普通数据对待（能按发布页的编辑/删除开关处理）。不要批量补审批。

---

## 5. 流程设计页长什么样

流程必须在前端 **画布设计器** 里配：左侧拖节点、中间连线、点节点或点线出右侧属性。不能改成「只上移下移的清单」——清单画不出分支。

页面结构对齐表单设计：左（节点）+ 中（画布）+ 右（属性）。顶栏是保存草稿、发布、启用流程。

实现上画布用 **LogicFlow 2.x**（安装时取当时的最新 2.x，不要锁死某个 patch）。审批 / 分支 / 结束用官方 **`@logicflow/vue-node-registry`** 把 Vue 单文件组件注册成节点，节点里面可以用 Element Plus。左侧拖拽、右侧属性、顶栏按钮仍是普通 Vue + Element Plus，不要整页都塞进画布。这是二维连线，Flex 做不了，属于现有「优先 Flex」规则的例外。

存库仍是本文的 `nodes` + `edges`（含坐标）。打开设计器时转成 LogicFlow 的图数据；保存时再转回，不要把 LogicFlow 内部字段原样当产品协议。抽屉里的只读小流程图可以再用同一个库只读渲染，或按快照自己画，不要另引第三套画布。

### 5.1 左侧：可拖的节点

| 名称 | 拖上去干什么 |
|---|---|
| 审批 | 要人处理的一步 |
| 分支 | 按表单字段走不同连线（菱形） |
| 结束 | 流程到此通过 |

**开始** 不进左侧：新建流程时画布上已经有一个开始，**只能有一个，不能删、不能再拖第二个**。

从左侧拖到画布空白处落下即新增；落到已有节点上不合并。节点可拖动位置，位置随流程草稿一起保存，刷新画布还在原处。

### 5.2 中间：画布和连线

- 每个节点有出线点、入线点。按住出线点拖到另一个节点的入线点，松开即连上。
- 连线可以点选、删除（Delete 或线上的删除）。
- 点节点：右侧是该节点属性。点连线：右侧是这条线的条件（见 §5.4）。点画布空白：右侧提示「选择一个节点或一条连线」。
- 画布支持平移、滚轮缩放；提供「适应画布」。
- **开始** 必须有且仅有一条出线。
- **审批** 必须有一条出线（指向下一个审批、分支或结束）。
- **分支** 至少两条出线：其中恰好一条标成「其他情况」，其余每条都要配条件。
- **结束** 不能有出线，可以有多条入线（几条支路汇到同一个结束）。
- 允许有多个结束（都表示「已通过」）。驳回不靠连到结束，靠审批人点「驳回」。
- 本期 **不允许环**（连回去形成圈）。发布时若从某点能沿连线走回自己，提示「流程不能绕回已经走过的节点」。
- 本期 **不允许并行**：同一时刻只在一个审批节点等人。两条出线是「只走其中一条」，不是两条同时批。不要做「同时发给经理和人事」。

没连线的节点、从开始走不到的节点、走到尽头不是结束的节点，发布时分别提示，见 §5.6。

### 5.3 右侧：节点属性

**审批节点**（点审批块）：

1. **节点名称**：文本，必填。默认「审批1」「审批2」。
2. **审批人**（必配，见 §7）。
3. **多人审批方式**（单选，默认 **或签**）：
   - **或签**：名单里**任一人**点「通过」，本节点通过，其余人的待办取消；**任一人**点「驳回」，本节点驳回，其余待办取消。
   - **会签**：名单里**每个人都必须**点「通过」，本节点才通过；**任一人**点「驳回」，本节点驳回，其余待办取消。还有人未批时，已批的人待办进「已办」，流程仍停在本节点，不往下走。
   - 只有一名审批人时，两种模式效果相同。
   - 存库字段 `signMode`：`any` = 或签，`all` = 会签。
4. **本节点字段权限**（见 §12）：每个主表字段（含标签页里的）可选「可编辑 / 只读 / 隐藏」。默认全部只读。发起填写不看这张表。
5. **审批意见**：通过时「选填 / 必填」，默认选填；驳回时固定必填。

**分支节点**（点菱形）：

1. **节点名称**：默认「分支」，可改成「按请假类型」方便认。
2. 不配审批人、不配字段权限。分支只负责选下一条线，不派人、不停留。

**开始 / 结束**：名称固定，右侧只说明「提交后从这里出发 / 走到这里即已通过」，没有审批人。

### 5.4 右侧：连线条件

点的是线，不是节点。

- **从开始、审批出来的那一条线**：不用配条件（它们各只有一条出线）。右侧显示「从「部门审批」到「人事备案」」，可改线上的短标题（画布上沿线显示，如「通过后」），可不填。
- **从分支出来的线**：
  - 必须指定：要么是 **其他情况**（本分支有且仅有一条），要么是 **满足以下条件**。
  - 条件配法和表单里下拉「选项过滤」同一套操作习惯：可多条；多条之间选「全部满足」或「任一满足」。
  - 一行：左边选**当前表单字段**（主表，含标签页；不要子表列）→ 中间选关系 → 右侧自定义值或当前表单另一个同类型字段。
  - 字段类型和关系对齐现有过滤：文本（等于 / 不等于 / 包含 / 为空…）、数字、日期、单选/下拉、人员/部门（等于、为空）。本期不做「动态相对日期」那种复杂项，够用请假类型、天数即可。
  - 线上显示条件摘要，如「请假类型 = 事假」；「其他情况」就显示这三个字。

运行时：按画布上**出线的顺序**（右侧可「上移 / 下移」调整判断顺序）一条一条看，**第一条满足的就走**，都不满足走「其他情况」。不要两条都走。

条件里用到的字段被表单设计删掉：发布时提示「分支「按请假类型」的连线用了已删除的字段」；已发布的旧版本实例仍按快照里的条件算，字段没值就当不满足，落到「其他情况」。

### 5.5 顶部操作

- **保存草稿**：只存流程定义（节点、线、坐标），工作台仍按「有没有已发布版本」判断是不是流程表单。
- **发布**：校验通过后生成/更新已发布版本，之后新提交用这个版本。
- **启用流程**：开关。关掉等于「这张表暂时不当流程表单」，已发布版本保留，再打开不用重配。

未发布过就关掉启用：表单仍是普通表单。

### 5.6 发布前要拦住的

画布：

- 没有开始，或开始不止一个
- 开始没有出线，或出线多于一条
- 没有任何审批节点
- 没有任何结束
- 某个审批没有名称或没有出线
- 某个分支出线少于 2 条，或没有恰好一条「其他情况」，或非默认线没配条件
- 存在从开始走不到的节点（提示节点名称）
- 存在走下去到不了任何结束的节点
- 存在环
- 结束上还连了出线

审批人：

- 某个审批没有审批人
- 审批人规则指向的角色已停用、人员已停用或已删
- 审批人规则选「表单内人员字段」，但字段已从画布删掉

表单字段以后又改了（增删改标题）：已发布流程仍可用。新增字段在老节点权限里视为「只读」。被删掉的字段 key 在权限表里保留无害，运行时找不到就忽略。人员字段被删导致某节点审批人解析不到人：待办派不出去，实例进「异常」（见 §16.5）。

---

## 6. 节点与连线（本期做哪些）

| 画布上是什么 | 本期 |
|---|---|
| 开始 | 做。提交后从这里沿唯一出线往下走 |
| 审批 | 做。停下来派人 |
| 分支 | 做。不停留，按条件选一条出线 |
| 结束 | 做。走到这里即已通过。可以多个 |
| 连线 | 做。从出线点拖到入线点；分支上的线带条件 |
| 抄送 | 不做 |
| 同时走两条（并行会签两条线） | 不做 |
| 加签 / 转办节点 | 不做（转办是以后的操作，不是节点） |

没有分支、只有「开始 → 审批 → 审批 → 结束」也完全合法，就是每步一条线的直路。

---

## 7. 审批人怎么定

审批人最终都要变成**具体用户 id 列表**，再给每人生成一条待办。

### 7.1 本期可选的三种来源（可组合）

属性里用和人员选择类似的配置，三种可以同时勾，结果取**并集去重**：

1. **指定人员**：从组织里勾人（复用登录可用的选人弹框，不要走管理后台 `/admin/*`）。
2. **指定角色**：选一个或多个启用中的角色。运行时取「拥有这些角色且账号启用」的全部用户。
3. **表单内的人员字段**：下拉当前表单里的人员单选 `member` 或人员多选 `member-multiple`（含标签页里的，不含子表列）。提交时读这条数据里已选的人。字段为空则派发失败（§16.5）。

「发起人自己」不单独做一项：若需要，把发起人加进指定人员，或在表单里用人员字段填自己。

### 7.2 本期不能选的（组织里还没有）

管理后台组织规格写明：一个人只属于一个部门，**没有部门负责人、没有汇报关系**。因此本期**不做**：

- 部门主管 / 上级
- 发起人部门的负责人
- 连续多级上报

以后组织里真补了「部门负责人」，再在审批人来源里加一项，不要先做假数据。

### 7.3 派发时的人名单

- 停用、已删除的用户：不进入待办。
- 三种来源合并后 **0 个人**：不能生成空待办。提交或流转到该节点时失败，提示「节点「xxx」没有可用的审批人」，实例保持上一状态（刚提交则记录已写入但实例进入「异常」见 §16.5）。
- 名单里包含发起人：允许自己审自己，不做自动跳过。
- 同一人因角色+指定人员出现两次：只生成一条待办。

---

## 8. 发起人：草稿、提交、撤回

工作台打开流程表单时，「添加数据」这块还在，底部按钮改成：

- **保存草稿**
- **提交**
- 取消（和现在一样，不写库）

不再单独显示普通的「保存」当正式入库。

### 8.1 保存草稿

- 走现有必填校验：**草稿也要过必填**，和现在点保存一样。不做「草稿允许空着」——否则数据管理里会多出大量半截数据，和现有填报习惯不一致。
- 写入 Mongo 填报集合 `frm_{formId}`，和普通保存同一套 `data`、创建人、流水号（流水号仍是第一次落库时生成，之后不再变）。
- 流程状态记为 **草稿**。没有待办，审批人看不见。
- 发起人可在「我发起的」或数据管理里再打开，继续改，再存草稿或提交。
- 草稿可以删除（发布页「删除」开关打开时）。

### 8.2 提交

- 同样先过必填和子表等现有校验。
- 若还没有记录：先入库再开流程。若已是草稿：更新字段再开流程。
- 读取**当前已发布**的流程版本，创建流程实例。从开始沿连线走：碰到分支就按这条数据算条件，直到落到第一个**审批**节点，给这些人派待办。若从开始一路只碰到分支就到了结束（没经过任何审批）：直接已通过——发布时应尽量避免这种图，但引擎仍按连线执行并提示「未经过审批即结束」。
- 状态变为 **审批中**（或上一句那种直接已通过）。
- 成功后提示「已提交，等待「部门审批」」（名称取实际落到的那个审批节点），工作台可以清空表单继续填下一张（和现在保存成功后清空一致）。

没有已发布流程却点了提交：按普通表单保存（防御；界面上普通表单不应出现提交按钮）。

### 8.3 撤回

- 只有**发起人**能撤回。
- 只有状态是审批中，并且**还没有任何审批人点过「通过」**时可以撤回（第一个节点有人驳回过也不允许撤回，应走改完再提交）。
- 撤回后：未处理待办全部取消；状态变回 **草稿**；审批记录里留一条「发起人撤回」。发起人可改后再提交（新开一轮待办，仍是同一条数据、**同一个实例继续**，历史留着）。

审批中不能删除这条数据，只能撤回成草稿再删。

---

## 9. 审批人：通过、驳回

审批人从「待办」点进一条，打开的是**右侧抽屉**，复用现在的数据详情抽屉（`FormRecordDetailDrawer`），不要另做一套填报表。

抽屉里多出的东西：

- 顶部：只读的小流程图（按提交时的画布缩略，当前审批节点高亮，已经走过的节点/连线用另一种颜色）。不要做成只能表示一条直线的步骤条，否则分支看不出来。
- 表单区：按**当前节点字段权限**显示；可编辑的字段能改，改完随「通过」一起保存。
- 底部左侧：审批意见输入框（按节点配置是否必填）。
- 底部右侧：**通过**、**驳回**、关闭。没有「编辑 / 保存」那套普通详情按钮。

同一条待办只能处理一次。两个人同时打开**同一条**待办：先提交的生效，后提交的提示「这条待办已处理」。**或签**下两人各有一条待办、同时点通过：先提交的那条让节点通过并取消另一条。**会签**下两人各有一条待办、同时点通过：两条都可以成功，都记已办；最后一人提交通过后节点才往下走。

### 9.1 通过

- 校验可编辑字段的必填（隐藏字段不校验；只读字段用库里已有值）。
- 意见按节点配置。
- 把可编辑字段的改动写回这条 Mongo 数据（只更新这些 key，别的字段不动）。
- 按节点 **多人审批方式**（§5.3）分支：
  - **或签**：本节点其余未处理待办取消；沿**本节点唯一的那条出线**继续走（见下）。
  - **会签**：只把当前这条待办标为完成。若同节点还有 `pending` 的待办，**不**取消、**不**往下走，界面提示「已提交，等待其他审批人处理」。若这是本节点最后一条待办被通过，再沿出线继续走。
- 沿**本节点唯一的那条出线**继续走：中间若是分支，按**当前库里的数据**（含本次通过刚写回的可编辑字段）选线；落到下一个审批则派待办；落到结束则状态 **已通过**。

### 9.2 驳回

- 意见必填。
- 本节点其余未处理待办取消（**或签、会签相同**：任一人驳回即整节点驳回）。
- 状态变为 **已驳回**。
- 不自动改业务字段（审批人在驳回前改的可编辑字段：若已点驳回，**不写入**那些改动，避免「驳回却改了请假日期」。驳回只写意见和历史）。
- 发起人在「我发起的」打开这条，表单可改，底部是「保存草稿」和「再次提交」。再次提交后状态回到审批中，**从开始重新沿连线走**（用改过的新数据重新判断分支）。历史上已经点过的通过/驳回还在，小流程图从开始重新高亮即将到达的节点。

本期不提供「驳回到上一步」「驳回后结束不能再交」。

### 9.3 本期没有的操作

转办、加签、退回指定节点、审批中沟通@人、批量通过。需要改人时：管理员改节点配置只影响新提交；当前待办仍是已派发的那些人。

---

## 10. 待办、我发起的、已办

流程表单不能只靠「数据管理」找自己的审批。工作台在这张表上，除「添加数据 / 数据管理」外增加三个页签（仅当该表是流程表单时出现）：

| 页签 | 列出什么 | 点进去做什么 |
|---|---|---|
| 待办 | 派给我、还没处理的任务 | 打开审批抽屉（§9） |
| 我发起的 | 我创建的流程数据（草稿、审批中、已通过、已驳回） | 草稿/已驳回可改可再交；审批中可撤回（满足 §8.3）；已通过只读（除非 §11 允许改正式数据） |
| 已办 | 我点过通过或驳回的任务 | 只读再看当时的数据和意见 |

三个列表都要有：提交时间或到达时间、当前节点名称、流程状态、发起人。分页与项目其它列表相同（每页 10 / 20 / 50 / 100）。

应用级「所有表的待办汇总」本期不做，先按表看。以后做应用首页待办再立项。

页签顺序：添加数据、待办、我发起的、已办、数据管理。表单发布里现有的「添加数据 / 数据管理」对调，只对调这两项，中间三个固定插在添加数据和数据管理之间。

---

## 11. 数据管理里看到什么

数据管理列表比普通表单多一列 **流程状态**（草稿 / 审批中 / 已通过 / 已驳回；没有实例的历史数据此列为空或「—」）。列设置里可关掉这一列，默认开。

| 操作 | 流程表单怎么处理 |
|---|---|
| 新增 | 仍走工作台添加数据（草稿/提交），数据管理「新增」同样进带提交按钮的抽屉 |
| 点行看详情 | 能看。审批中的详情只读，底部可看到进度和历史意见；当前用户若是待办审批人，按钮与待办抽屉相同 |
| 编辑 | 草稿、已驳回：发起人可编辑。审批中：不能当普通编辑打开（没有铅笔、工具栏编辑对勾选的审批中数据提示「审批中的数据请到待办或我发起的处理」）。已通过：若发布页「编辑」关闭则不能改；若打开，允许改正式数据且**不再走一遍审批**（改的是已生效业务数据）。这个行为要在发布页补一句说明，避免误以为改已通过还会重审 |
| 删除 | 草稿可删。审批中不可删。已通过 / 已驳回：跟发布页删除开关 |
| 单元格内联编辑 | **整表关闭**。流程表单不提供列表里改格子，避免绕过节点权限 |
| 导入 / 下载模版 | **关闭**。导入会绕过审批。发布页这两项对流程表单强制关且不可再开 |
| 导出 | 仍按发布页（现在导出本身未做，保持不变） |

筛选：便捷筛选暂不包含流程状态（现有便捷筛选类型名单不变）。列表上方加一个流程状态下拉（全部 / 草稿 / 审批中 / 已通过 / 已驳回），只对流程表单出现。

---

## 12. 字段谁能改、谁只能看

分两套，不要混：

**发起填写（添加数据、草稿、已驳回再交）**

- 仍用字段自己的：必填、禁用、是否可修改、流水号只读等。
- 不看审批节点上的权限表。

**某个审批节点打开抽屉时**

- 可编辑：审批人能改，通过时写回。
- 只读：能看见，改不了。
- 隐藏：抽屉里不渲染（标签页里某字段隐藏则该格不出现；整个页签的字段都隐藏时，页签仍在，里面为空——不做自动藏页签，以免和现有标签页行为纠缠）。
- 子表单 `subform`：本期整表只读，不能在审批里增删改行。节点权限里子表只出现「只读 / 隐藏」，没有「可编辑」。这与子表规格「流程节点上的子表权限另立项」一致。
- 关联子表单 `relate-subform`：本来就是只读表格，权限里不出现（和分割线一样不当填报项）。
- 关联数据 `relate`、选择数据 `data`：可以标可编辑，审批人按现有控件重选。

登录人姓名、登录人部门、流水号：权限里可只读或隐藏，不能标可编辑。

---

## 13. 流程改了、老单据怎么办

流程定义带版本号，从 1 递增。

- **保存草稿**：改未发布的设计稿，不影响任何人。
- **发布**：版本 +1，成为新的已发布版本。之后的新提交、草稿首次提交，都用新版本。
- **已经在审批中的实例**：钉死在**提交当时**的版本。设计器里删掉「人事备案」或改审批人，不影响这张正在走的请假单。
- **已驳回后再次提交**：仍用**该实例钉死的旧版本**，不悄悄换成新流程，避免同一条单据前后节点对不上。发起人若要走新流程，只能删掉这条（若允许）再新建。
- **已通过的数据**：没有进行中的实例，流程改了也不会重审。

界面上流程设计页显示「当前已发布版本 v3。审批中的 2 条仍按提交时的版本走。」

---

## 14. 和现有功能怎么相处

| 能力 | 流程表单下的约定 |
|---|---|
| 流水号 `serialNumber` | 第一次保存草稿或直接提交时生成，之后再提交、审批改字段都不变 |
| 数据联动 | 发起填写、审批人改可编辑的条件字段时，照常带出。列表内联编辑本来就不跑联动，流程表单也没有内联编辑 |
| 选择数据 `data` / 关联数据 `relate` | 当**本表是源表**时：别的表弹窗选记录，只列出流程状态为已通过的（没有流程的普通表不受影响） |
| 关联子表单 `relate-subform` | 主表详情里列子表数据时，子表若是流程表单，只列出已通过的行 |
| 下拉「其他表数据」 | 选项取的是字段值不是记录资格，**不过滤**流程状态（和现在「存的是值不是关系」一致） |
| 人员 / 部门 | 审批人指定人员和表单内人员字段，用同一套组织接口 |
| 标签页 `tabs` | 流程可绑在整张表上，不能给某个页签单独绑流程 |
| 子表单 `subform` | 发起时按现有规则填；审批里只读（§12） |
| 表单发布「新增/编辑/删除」 | 仍有效，但叠加 §11 的流程状态限制；导入模版对流程表单强制关 |
| 应用目前按创建者使用 | 待办、发起仍在本应用内。应用级/表单级访问权限（TODO P7）没做之前，能进工作台的人就能发起；审批人即使平时很少进这张表，只要被派了待办，也应能打开该条审批（接口按 task 授权，不要求是应用创建者以外的新角色——若当前接口只有 owner 能读记录，本期必须把「待办审批人读这一条」放开，否则指定角色的人打不开请假单） |

最后一点是实现硬条件：现有 `FormRecordService` 按应用 owner 鉴权。流程上线时，读单条记录要增加：**当前用户是该记录流程的发起人或任一未完成/已完成待办的审批人**，也可以读。写仍只允许引擎和规则里允许的人。

---

## 15. 库里怎么存

配置走 MySQL，填报仍走 Mongo，和现有混合存储一致。**不要**为流程再给每张表建一张业务表。

### 15.1 MySQL（流程自己的表）

表名实现时按项目 plural / TypeORM 习惯定，这里说清有哪些东西。

**流程定义**（一张表单一份「当前设计稿 + 已发布版本」即可用两行策略，或一行里带 draft_json + published_json + version。推荐一张表）：

| 人怎么理解 | 存什么 |
|---|---|
| 属于哪个应用、哪张表 | `app_id`、`form_id`，`form_id` 唯一 |
| 有没有启用 | `enabled` |
| 设计中的流程图 | `draft_graph`（JSON：节点 + 连线 + 坐标） |
| 已发布的流程图 | `published_graph`（同上，可空表示从未发布） |
| 已发布版本号 | `published_version`，从未发布为 0 |
| 何时发布 | `published_at` |

流程图 JSON 示例（人先看含义；`x` / `y` 是画布坐标，刷新后节点还在原处）：

```json
{
  "nodes": [
    { "key": "start", "type": "start", "title": "开始", "x": 240, "y": 40 },
    { "key": "br1", "type": "branch", "title": "按请假类型", "x": 240, "y": 140 },
    {
      "key": "n1",
      "type": "approve",
      "title": "部门审批",
      "x": 80,
      "y": 280,
      "approver": { "userIds": [], "roleIds": [2], "memberFieldKeys": [] },
      "signMode": "any",
      "commentRequiredOnApprove": false,
      "fieldAccess": {
        "field_leave_type": "readonly",
        "field_reason": "editable"
      }
    },
    {
      "key": "n2",
      "type": "approve",
      "title": "人事备案",
      "x": 240,
      "y": 420,
      "approver": { "userIds": [9], "roleIds": [], "memberFieldKeys": [] },
      "signMode": "all",
      "commentRequiredOnApprove": false,
      "fieldAccess": {}
    },
    { "key": "end", "type": "end", "title": "结束", "x": 240, "y": 540 }
  ],
  "edges": [
    { "key": "e1", "from": "start", "to": "br1" },
    {
      "key": "e2",
      "from": "br1",
      "to": "n1",
      "title": "事假",
      "sort": 1,
      "isDefault": false,
      "when": {
        "logic": "all",
        "items": [{ "key": "field_leave_type", "op": "eq", "value": "事假" }]
      }
    },
    { "key": "e3", "from": "br1", "to": "n2", "title": "其他情况", "sort": 2, "isDefault": true },
    { "key": "e4", "from": "n1", "to": "n2" },
    { "key": "e5", "from": "n2", "to": "end" }
  ]
}
```

`fieldAccess` 缺省的字段按只读。`hidden` / `readonly` / `editable` 三值。`when` 的配法和选项过滤同一套「全部/任一 + 字段 + 关系 + 值」。`sort` 越小越先判断。

**流程实例**（一条填报最多一条实例）：

| 人怎么理解 | 存什么 |
|---|---|
| 哪条数据 | `form_id` + `record_id`（Mongo 那条 id 字符串） |
| 按哪个流程版本走 | `definition_version` + 当时的 `graph` 快照（整份 JSON 拷进来，版本被以后的发布覆盖也不丢） |
| 谁发起的 | `initiator_id` |
| 现在到哪 | `status`：`draft` / `running` / `approved` / `rejected` / `error`。撤回回到 `draft`，不另做「已撤回」 |
| 当前审批节点 | `current_node_key`，停在审批上等人为该审批的 key；草稿和结束时可空。分支不算「当前节点」 |
| 已经走过的节点 | `visited_node_keys`（数组，给小流程图上色） |
| 有没有人已经点过通过 | `has_approved`，给撤回判断用 |
| 时间 | `started_at`（首次提交）、`ended_at` |

草稿：可以「有 record、有 instance 且 status=draft」，提交时同一条 instance 改成 running。这样撤回、再提交都对着同一行。保存草稿时就创建 instance，避免 record 和流程对不上。

**待办任务**

| 人怎么理解 | 存什么 |
|---|---|
| 属于哪个实例、哪个节点 | `instance_id`、`node_key` |
| 给谁 | `assignee_id` |
| 状态 | `pending` / `done` / `cancelled` |
| 人点了什么 | `action`：`approve` / `reject`，未处理为空 |
| 意见 | `comment` |
| 时间 | `created_at`、`finished_at` |

**历史**（只追加，待办取消也写一条系统记录以免对账只靠任务表）

列表「进度 + 意见」从任务表就能拼：已完成的通过/驳回 + 取消原因（系统：或签其他人已通过 / 或签其他人已驳回 / 会签节点已驳回 / 发起人撤回）。若展示够用，可以不另建 history 表；**本期用任务表当历史**，少一张表。取消的任务 `action` 为空、`status=cancelled`，界面显示「未处理（已取消）」。

### 15.2 Mongo 填报文档

`frm_{formId}` 文档根上增加冗余，方便列表筛，**不以它为权威**：

| 键 | 含义 |
|---|---|
| `workflowStatus` | 与实例 `status` 同步：`draft` / `running` / `approved` / `rejected` / `error`。普通表单或历史无流程数据：**不要这个键** |
| `workflowInstanceId` | MySQL 实例 id，没有实例则无此键 |

写这两项只允许流程引擎在同一业务操作里更新。填报的 `data` 形状不变。筛选「只已通过」给关联数据弹窗用时，查 Mongo：`workflowStatus` 不存在 **或** 等于 `approved`——不对：普通表没有该键应能被选；流程表只有 `approved` 能被选。

实现上弹窗查询要带表单是否流程表单：

- 普通表：现有 query，不加条件
- 流程表：`workflowStatus = approved`

不要用「没有这个键也算通过」。

管理员作废整单本期不做。撤回只回到草稿。

### 15.3 事务

一次「通过」要改：Mongo 字段、Mongo 状态、MySQL 任务、MySQL 实例。没有跨库事务。

顺序约定：

1. 先在 MySQL 把待办从 pending 改成 done（带条件更新，防并发）
2. 再写 Mongo 的 data 补丁和 `workflowStatus`
3. 再派下一节点待办或把实例标结束

若 2 失败：待办已是 done，实例仍 running，进入 **异常**（§16.5），提供「重试流转」而不是让用户再点一次通过。若 1 失败：整单当没点过，用户重试。

---

## 16. 引擎规则（按人点的顺序）

### 16.1 状态

```text
保存草稿 → draft
提交 / 再次提交 → running
沿连线走到结束 → approved
任一节点驳回 → rejected
撤回 → draft（has_approved 仍为假）
派发不到人 / 写库中断 → error（界面显示「异常」，管理员或发起人可点重试）
```

不允许的跳转（直接拒绝）：`approved` → 再提交；`running` → 普通编辑保存；`draft` → 审批人通过。

### 16.0 沿连线走到下一个停留点

提交、通过、再次提交之后，都要「从某个点往下走，直到必须等人或结束」：

1. 从指定节点的出线出发（提交、再次提交从开始的出线出发；通过从刚通过的那个审批的出线出发）。
2. 下一格是 **分支**：按出线 `sort` 依次算 `when`，命中第一条；都不中走 `isDefault` 那条。条件里的值读**此刻** Mongo 里的 `data`（通过时含刚写回的补丁）。
3. 下一格是 **审批**：停下来派待办，写入 `current_node_key`，并把该节点推进 `visited_node_keys`。
4. 下一格是 **结束**：状态改为已通过，清空当前节点。
5. 下一格缺失、条件算不出来又没有「其他情况」、或走到环（快照若被改坏）：进异常，原因写清。

分支本身不进待办。经过的分支 key 也写入 `visited_node_keys`，小流程图才能把走了哪条岔画出来。

### 16.2 多人审批方式（或签 / 会签）

审批节点快照里带 `signMode`（`any` | `all`），缺省当 `any`（或签）。同一 `node_key` 下可能有多条 `pending` 任务（审批人并集去重后每人一条）。

**或签（`signMode = any`）**

- 任一条待办变成 `done` + `approve` 后，其余同节点 `pending` 改为 `cancelled`，取消原因记「或签其他人已通过」。
- 任一条变成 `done` + `reject` 后，其余 `pending` 改为 `cancelled`，取消原因记「或签其他人已驳回」。
- 通过时写 Mongo、沿出线往下走，与 §9.1 一致。

**会签（`signMode = all`）**

- 任一条变成 `done` + `reject` 后，其余同节点 `pending` 改为 `cancelled`，取消原因记「会签节点已驳回」；实例 `rejected`，与 §9.2 一致。
- 任一条变成 `done` + `approve` 后：**若**同节点仍有 `pending`，只更新本条，实例 `current_node_key` 不变，不派下一节点。
- **若**本条通过后同节点已没有 `pending`（所有人都已通过），再写 Mongo、取消无（都已 done）、沿出线往下走。
- 并发：两人同时通过且是不同 `assignee` 的两条待办，两条都应能 `done`；引擎用「本节点 `pending` 计数是否为 0」决定是否流转，必须用条件更新防重复推进。

**只有一名审批人**

- `signMode` 任意，行为与或签通过 / 驳回相同（没有「等其他人」）。

### 16.3 再次提交

`rejected` → 校验 → 更新 data → `running`，`has_approved` 置回假，按**实例快照里的图**从开始重新走 §16.0。旧任务都已是 done/cancelled，新任务新 id。`visited_node_keys` 从本轮重新记（上一轮走过的仍能从旧任务看历史，小流程图「本轮路径」用新数组）。

### 16.4 并发

完成待办必须用「id + status=pending + assignee=当前用户」更新，影响行数 0 则提示已处理。不要先读再写无条件更新。

### 16.5 异常（error）

出现时机：提交或流转时审批人名单为空；或 MySQL 已落点、Mongo 失败。

界面：

- 发起人在「我发起的」看到状态「异常」和原因（「节点「部门审批」没有可用的审批人」/「写入数据失败，请重试」）
- 提供 **重试**：重新解析审批人并派待办，或重放未完成的 Mongo 写入
- 提供 **撤回成草稿**（与 §8.3 相同条件放宽：error 且无人成功通过时也可以撤）

不要静默停在审批中却没有任何待办。

---

## 17. 人在系统里会碰到的接口（先说事，再写路径）

鉴权：已登录。读/写应用与表单仍先走现有应用校验；**读单条填报**按 §14 对发起人、待办审批人放行。

以下路径为约定，实现时可放在 `/api/apps/:appId/forms/:formId/workflow/...` 下，与现有 `/apps/:appId/forms/:formId/records` 并列。

| 人做的事 | 方法与路径（草案） |
|---|---|
| 打开流程设计，看到草稿和已发布版本 | `GET .../workflow` |
| 保存流程草稿 | `PUT .../workflow/draft` |
| 发布流程、开关启用 | `POST .../workflow/publish`、`PATCH .../workflow`（enabled） |
| 保存草稿数据 | 仍用 `POST .../records`，body 带 `submit: false` 或沿用现接口另加 `POST .../records` + `POST .../workflow/start` 二选一。**推荐一个入口**：`POST .../records` 增加可选 `intent: 'draft' \| 'submit'`，普通表单忽略该字段，行为与现在一致 |
| 提交 / 再次提交 | 同上，`intent: 'submit'`；已有 id 则 `PATCH .../records/:id` 带 `intent` |
| 撤回 | `POST .../workflow/instances/:instanceId/cancel` |
| 我的待办 / 我发起的 / 已办 | `POST .../workflow/tasks/query`（kind=todo\|mine\|done） |
| 通过 / 驳回 | `POST .../workflow/tasks/:taskId/complete`，body：`action`、`comment`、可编辑字段的 `data` 补丁 |
| 重试异常 | `POST .../workflow/instances/:instanceId/retry` |
| 数据管理列表带状态 | 现有 `records/query` 增加可选 `workflowStatus`；返回每条带 `workflowStatus` |
| 关联数据弹窗只选已通过 | 现有 query 增加服务端判断：源表是流程表单则强制 `workflowStatus=approved` |

`intent` 加在现有 records 上，是为了流水号、校验、coerce 仍走一条 `FormRecordService`，引擎只在保存成功后启动或推进。

---

## 18. 前端改哪些地方（按页面）

不写函数名堆砌，按人点的入口：

1. **表单设计顶栏**：增加「流程设计」页签；整页是左节点 + 中画布（拖节点、拉线）+ 右属性。
2. **工作台该表**：流程表单显示待办 / 我发起的 / 已办；添加数据底部改按钮。
3. **数据详情抽屉**：按是否有待办、是否发起人、流程状态，切换底部按钮；顶部是只读小流程图，不是单行步骤条。
4. **数据管理列表**：流程状态列、状态下拉、关闭内联编辑和导入。
5. **选择数据 / 关联数据弹窗**：源表是流程表单时只出现已通过（后端过滤，前端不另做一套假分页）。

---

## 19. 本期不做

| 项 | 说明 |
|---|---|
| BPMN 引擎（Camunda 等） | 自研画布 + 自研沿连线走；不引入外部引擎 |
| 自定义投票比例（如 2/3 人通过即可） | 只有或签、会签两种 |
| 抄送、子流程、并行两条线同时批 | — |
| 转办、加签、退回指定节点、批量审批 | — |
| 部门主管 / 多级上报 | 组织还没有负责人 |
| 站外通知（邮件、企微） | 待办列表即入口 |
| 应用级待办汇总、移动端审批 | — |
| 审批里编辑子表行 | 子表只读/隐藏 |
| 流程表单导入 Excel | 会绕过审批 |
| 已通过后自动再走一遍流程 | 发布页若允许编辑，直接改数 |
| 流程权限单独一套角色 | 复用组织角色 + 指定人 + 表单人员字段 |
| 数据权限（按部门看单据） | TODO P7，本期发起人/审批人/能进表的人 |
| 打印审批单、签章 | — |

---

## 20. 分期（做完一期要能真正请假）

**第 1 期（本文必须一次做完才能叫流程表单）**

- 流程设计器：拖审批 / 分支 / 结束，从开始连线，分支线上配条件，保存坐标
- 发布校验（孤点、环、缺「其他情况」、缺审批人等）
- 三种审批人、**或签 / 会签**（节点属性可配）、启用开关、版本钉死
- 引擎按连线 + 分支条件走；草稿 / 提交 / 撤回 / 通过 / 驳回 / 再次提交（含会签「等所有人」）
- 待办、我发起的、已办；抽屉里只读小流程图
- 节点字段权限（主表，含子表只读或隐藏）
- 数据管理状态列与限制；关联/选择数据只挑已通过
- 待办人可读单条记录；异常与重试

**第 2 期（常用增强，另开规格）**

- 抄送、并行（同时走两条）
- 转办、驳回指定节点
- 应用级待办汇总
- 审批里按节点改子表

**第 3 期**

- 部门负责人、超时催办、站外通知
- 流程模板跨表复用、导入导出流程图

---

## 21. 验收（按请假单点）

1. **还没发布流程**：请假单工作台仍是「保存」，和现在一样，没有待办页签。
2. **画布上拖出 §2 那张图（开始→分支→事假进部门审批、病假进人事→汇合人事→结束），配好审批人并发布、启用**：工作台出现待办等页签；添加数据底部是保存草稿、提交。没有连线或分支缺「其他情况」时点发布会被拦住。
3. **必填没填就提交**：仍提示请填写某字段，不进审批。
4. **保存草稿**：数据管理能看到一条「草稿」；待办里没有；关联数据从别的表来挑请假单，看不到这条。
5. **提交事假**：提示等待部门审批；部门经理角色下每人一条待办；张三还没有；发起人在我发起的里能撤回。
6. **另提一条病假**：不经部门审批，张三直接有待办。
7. **事假被经理通过**：意见选填可空；张三出现待办；其他经理待办消失；小流程图高亮人事备案。
8. **张三点开**：事由若节点标了可编辑，能改；改完通过；状态已通过；别的表关联数据能挑到这条。
9. **经理驳回事假**：必须写意见；发起人改成病假再提交；本轮不再出现经理待办，直接到张三。
10. **两人同时通过或签**：一人成功，另一人提示已处理。
11. **会签节点**（如人事备案 `signMode=all`，指定 A、B 两人）：A 通过后流程仍停在人事备案，B 的待办仍在；B 通过后才到结束。A 驳回后 B 的待办取消，状态已驳回。
12. **发布前删掉所有审批节点，或分支只留一条线**：不能发布。
13. **审批中改画布并发布**：进行中的那张请假仍按提交时的图走；新请的假按新图。
14. **审批中去数据管理点编辑、点单元格**：不能当普通数据改掉。
15. **角色里没人、或人员字段空着就提交**：不能变成「审批中但没人有待办」，应提示异常并可重试或撤回。
16. **关掉启用流程**：新填的请假又变成普通保存；上一张还在审批的继续审完。

---

## 22. 需要拍板的地方（写进本文的默认，可改）

下面这些已经按「能落地、和现有习惯接近」选定。若要改，改本文再实现，不要实现时另解释一套。

| 问题 | 本文默认 |
|---|---|
| 草稿要不要过必填 | 要，和现在保存一样 |
| 多人审批 | 或签、会签；节点属性里单选，默认或签 |
| 驳回后从哪重新走 | 从开始重新沿连线走（用新数据重新判断分支） |
| 已通过后发布页还开着编辑 | 允许改，不再审批 |
| 历史数据绑流程之后 | 不补流程，当普通数据 |
| 选记录弹窗 | 流程表只出已通过 |
| 下拉其他表数据 | 不过滤流程状态 |
| 流程设计器 | 画布：拖节点、拉连线、分支配条件 |
| 分支怎么选路 | 只走一条；按线的顺序匹配，否则走「其他情况」 |
| 并行两条同时批 | 不做 |
| 部门主管 | 不做 |

---

## 23. 和 TODO 的关系

做完第 1 期后，`docs/TODO.md` P5「流程表单」可勾选，并注明第 2 期另立项。P7 数据权限、应用/表单权限仍是流程之外的平台能力；流程第 1 期只把「待办人能打开这一条」补上。
