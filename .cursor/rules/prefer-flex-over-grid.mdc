---
description: 前端布局优先用 flex，只有 flex 做不到而 grid 做得到时才用 grid
globs: front/**/*.{vue,js,less,css}
alwaysApply: true
---

# 优先 flex，少用 grid

写或改 `front/` 下的布局时，**先用 flex**。只有 flex 实现不了、而 grid 能实现时，才用 `display: grid`。

不要为了“看起来整齐”或“以后可能多列”一上来就上 grid。

## 用 flex

横排、竖排、两端对齐、剩余空间拉伸、换行、间距：

```css
/* ✅ GOOD */
.toolbar {
  display: flex;
  align-items: center;
  gap: 8px;
}

.page {
  display: flex;
  flex-direction: column;
  min-height: 0;
}

.body {
  flex: 1;
  min-height: 0;
}
```

## 才用 grid

需要二维对齐、或同一行里按比例占多列（例如 12 栅格里字段占 1/4、1/3、整行混排）时：

```css
/* ✅ GOOD：flex 很难稳定做出可变列宽混排 */
.fill-grid {
  display: grid;
  grid-template-columns: repeat(12, minmax(0, 1fr));
}

.fill-field.is-w-quarter {
  grid-column: span 3;
}
```

```css
/* ❌ BAD：一行按钮、侧栏+主区、顶栏+内容，用 flex 就够 */
.toolbar {
  display: grid;
  grid-template-columns: auto 1fr auto;
}
```

已有页面里的 grid 不必专门改写；新写或改到相关布局时，按这条选。
