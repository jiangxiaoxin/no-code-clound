---
description: 新增或改完表单组件后，同步更新 docs/testcases 专项与联合用例
alwaysApply: true
---

# 新增组件后更新手工测试用例

实现或明显改动表单组件（尤其是容器、联动、选择数据、流水号、成员/部门这类复杂控件）后，**同一轮工作里**更新 `docs/testcases/`，不要只写规格或计划。功能没做完（调色板占位）不要写成可测功能；在组合用例「能力范围」里标明不要当功能测。

## 要改哪些文件

1. **专项**：该组件自己的 `docs/testcases/YYYY-MM-DD-<能力>-test-cases.md`（没有就新建）。覆盖设计、填报、校验、列表、导入等主路径。
2. **联合**：更新 `docs/testcases/2026-08-31-complex-form-manual-tests.md`（或后续总组合文档）。至少写清与已有复杂能力的互动：标签页、子表单、数据联动 / 选择数据、流水号。
3. **纲领**：更新 `docs/testcases/README.md` 的目录表、执行顺序、开始前准备、破坏性步骤、不要测清单。

规格仍放 `docs/superpowers/specs/`，不要把用例写进规格里。

## 联合测试至少想这些

- 能不能放进标签页 / 子表；不能的话拖入应被拒绝
- 未打开的 pane 里，必填、联动、占号是否仍生效
- 联动条件 / 带出目标 / 选择数据映射能不能引用它；列表 `flatten` 会不会多出不该有的列
- 校验失败是否跳到正确 pane；删除父容器后子内容是否一起没
- 工作台添加数据、数据管理、导入导出有没有特殊列或只读

```text
# ❌ BAD
做完成员选择只改了 design spec，testcases 里仍写「不要当功能测」

# ✅ GOOD
新建成员选择专项；组合用例补「成员在标签页 / 子表 / 联动条件」；README 执行顺序插入该专项
```
