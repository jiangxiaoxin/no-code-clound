---
description: 新增表单组件，或已有组件核心功能改变后，同步更新 docs/testcases 专项与联合用例
alwaysApply: true
---

# 新增或改完表单组件后更新手工测试用例

实现**新**表单组件，或**已有**组件的核心功能发生改变后，**同一轮工作里**更新 `docs/testcases/`，不要只写规格或计划。不是新组件也不能跳过：查询时机、选项来源、校验、能否进标签页/子表、入库展示、联动、权限这类用户可测行为变了，就要改对应用例。

功能没做完（调色板占位）不要写成可测功能；在组合用例「能力范围」里标明不要当功能测。只改样式、文案措辞、不影响测法的内部重构，不必为了对齐而扩写用例。

## 要改哪些文件

1. **专项**：该组件自己的 `docs/testcases/YYYY-MM-DD-<能力>-test-cases.md`（没有就新建）。覆盖设计、填报、校验、列表、导入等主路径；核心行为变了就改旧编号或补新编号，不要留过期期望。
2. **联合**：更新 `docs/testcases/2026-08-31-complex-form-manual-tests.md`（或后续总组合文档）。至少写清与已有复杂能力的互动：标签页 `tabs`、子表单 `subform`、数据联动 / 选择数据 `data`、流水号 `serialNumber`。
3. **纲领**：更新 `docs/testcases/README.md` 的目录表、**调色板中文名与 `type` 对照表**、执行顺序、开始前准备、破坏性步骤、不要测清单。用例里写控件时同时写调色板名称和字段 `type`（例如子表单 `subform`、下拉单选框 `select`）。

规格仍放 `docs/superpowers/specs/`，不要把用例写进规格里。

## 联合测试至少想这些

- 能不能放进标签页 / 子表；不能的话拖入应被拒绝
- 未打开的 pane 里，必填、联动、占号是否仍生效
- 联动条件 / 带出目标 / 选择数据映射能不能引用它；列表 `flatten` 会不会多出不该有的列
- 校验失败是否跳到正确 pane；删除父容器后子内容是否一起没
- 工作台添加数据、数据管理、导入导出有没有特殊列或只读

```text
# ❌ BAD
做完成员选择只改了 design spec，testcases 里仍写「不要当功能测」

# ❌ BAD
子表下拉何时请求源表已经改了，因为「不是新组件」就不改用例

# ✅ GOOD
新建成员选择专项；组合用例补「成员在标签页 / 子表 / 联动条件」；README 执行顺序插入该专项

# ✅ GOOD
已有子表单下拉查询时机变了：专项补 F-33；组合补 C-14；README 容易误判写上
```
