---
description: 文档和代码一律 UTF-8，并检查中文是否乱码
alwaysApply: true
---

# 使用 UTF-8，检查中文乱码

读写文档和代码一律用 **UTF-8**（无 BOM）。不要用系统默认的 GBK / ANSI 去读或写文件。

## 何时检查

新建、修改、复制、从计划拆 brief、生成 diff 之后，都要看一眼中文是否还是正常汉字。`docs/`、`.cursor/rules/`、`AGENTS.md`、源码字符串和注释、`.superpowers/sdd/` 都要查。

## 乱码长什么样

- UTF-8 被当成 GBK：`标签页` → `鏍囩椤?`，`请填写` → `璇峰～鍐`
- UTF-8 被当成 Latin-1：`复杂测试表` → `å¤ææµè¯è¡¨`

看到这类字、``、或中文句子读不通，就是编码错了，不要把乱码写进实现或测试。

## 怎么处理

- 以仍是正确中文的源为准（计划、规格、现有源码），不要对着乱码猜。
- 纠正编码，不要删文件；删之前仍须使用者点名确认。
- 用编辑器或 Node 按 UTF-8 读写。不要用会按系统代码页改写文件的管道（例如 PowerShell 默认 `Get-Content` / `Out-File`）再存回去。

```text
# ❌ BAD
把 brief 里的 '鏍囩椤?' 原样写进 tabsField.js

# ✅ GOOD
对照计划写成 '标签页1'；写完再打开文件确认汉字正常
```
