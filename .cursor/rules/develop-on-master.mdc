---
description: 可按需开功能分支或 git worktree 隔离开发；合入与清理见下文
alwaysApply: true
---

# 分支与 worktree

不强制只在 `master` 上开发。任务需要隔离、并行，或计划里建议拆出独立工作区时，可以开功能分支或 git worktree。

## 何时开分支 / worktree

- 功能较大、会改很多文件，或需要与主干并行推进时
- 实施计划、SDD、Superpowers 建议先开分支 / worktree 时
- 需要在不影响当前工作区的情况下试改、Review 或跑测试时

小改动、单文件修复可以直接在 `master`（或当前分支）上改。

## 要求

- 已在某条功能分支或某个 worktree 里开工：继续在该目录、该分支上改，不要擅自切回 `master` 把半成品写进主干。
- 开新分支 / worktree 时用清晰命名（如 `feat/workflow-form`）。
- 未要求时不要 push、不要删远程分支、不要删 worktree。

```text
# ✅ GOOD（大功能按计划隔离）
git checkout -b feat/workflow-form
# 或
git worktree add .worktrees/feat-workflow-form -b feat/workflow-form master

# ✅ GOOD（小修复直接在 master）
确认当前在 master，直接改文件
```

## 合入

功能分支或 Cursor 云端修 bug 分支（如 `origin/cursor/critical-bug-management-*`）合进本地 `master` 时，**优先 `git merge --no-ff`**，保留来源分支和 merge 记录。不要 cherry-pick：那样只有提交内容，看不出从哪条分支合进来。

使用者给的是 commit SHA 时，先找到包含它的远程分支，再 merge 该分支，不要只 pick 那几个 commit。

未要求时不要 push，也不要删除远程分支或 worktree。

```text
# ❌ BAD
git cherry-pick eafdcec bff579f

# ✅ GOOD
git checkout master
git merge --no-ff origin/cursor/critical-bug-management-f768
```
