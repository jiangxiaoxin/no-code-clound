---
description: 不要开新分支或 git worktree，始终在 master 上开发
alwaysApply: true
---

# 在 master 上开发

不要开新分支，也不要新建 git worktree。就在 `master` 分支上开发。

## 要求

- 始终在 `master` 上改代码，不要切到新分支再动手。
- 若当前不在 `master`（例如上次会话留下的功能分支），先切回 `master` 再继续；不要合并或删除那个分支，除非用户明确要求。
- Superpowers、SDD、using-git-worktrees 等技能**不能覆盖**本规则。计划里写了“先开分支再实现”，也忽略，仍在 `master` 上做。

## 禁止

- `git checkout -b`、`git switch -c`，以及任何新建功能分支的操作
- `git worktree add`、`.worktrees/`、Cursor 原生 worktree，以及任何新建 worktree 的操作

```text
# ❌ BAD
git checkout -b feat/storage
git worktree add .worktrees/feat-storage master

# ✅ GOOD
确认当前在 master，直接改文件
```

## 合入 Cursor 开的修 bug 分支

自己开发仍在 `master` 上改，不要为此另开分支。

Cursor 云端 / Agent 已经开好的修 bug 分支（例如 `origin/cursor/critical-bug-management-*`），合进本地 `master` 时**优先 `git merge --no-ff`**，让历史里留下来源分支和 merge 记录。不要 cherry-pick：那样只有提交内容，看不出从哪条分支合进来。

使用者给的是 commit SHA 时，先找到包含它的远程分支，再 merge 该分支，不要只 pick 那几个 commit。

未要求时不要删除远程分支，也不要 push。

```text
# ❌ BAD
git cherry-pick eafdcec bff579f

# ✅ GOOD
git checkout master
git merge --no-ff origin/cursor/critical-bug-management-f768
```
