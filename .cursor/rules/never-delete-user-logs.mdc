---
description: 用户自行添加的日志文件禁止删除，除非用户明确要求
alwaysApply: true
---

# 禁止删除用户日志

使用者自己添加的日志文件，任何情况下都不得删除、清空、覆盖或移入回收站。只有在使用者用明确语句要求删除时才可以动这些文件。

## 识别

- 常见路径：`*.log`、`logs/`、`*.log.*`，以及对话里使用者说明是自己加的日志。
- 拿不准是不是使用者添加的，一律当作“不能删”。

## 禁止

- 清理、整理、提交前检查、修 bug、重构时顺手删日志
- `rm`、`del`、`Remove-Item`、Delete 工具、清空文件内容、`>` 重写日志
- 把日志加入“顺便清理的无用文件”

```text
# ❌ BAD
删掉 logs/app.log，目录更干净

# ✅ GOOD
保留 logs/app.log；只有用户说“删除这个日志”时才删
```

“帮我清理项目”“提交前整理一下”“这些应该没用了”都不构成删除许可。必须出现明确对象，例如：“删除 `logs/app.log`”。
