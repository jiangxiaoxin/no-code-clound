---
description: 用户自己添加的注释和日志输出禁止删除或修改，除非用户明确要求
alwaysApply: true
---

# 禁止改动用户注释和日志输出

使用者自己写的注释、`console.log` / `console.debug` 等日志输出，不是无用代码。任何情况下都不得删除、改写、挪走或“顺便清理”。只有使用者用明确语句点名要求时才可以动。

## 识别

- 代码里已有的中文注释、TODO、说明某段为何这样写的 HTML/JS/TS 注释
- `console.log`、`console.debug`、`console.info`、`console.warn` 以及对话里使用者说明是自己加的日志
- 拿不准是不是使用者添加的，一律当作“不能动”

## 禁止

- 清理、整理、review、重构、修 bug 时顺手删注释或日志
- 把注释改成更短的说法、统一格式、挪位置
- 把日志标成“调试残留 / 无用代码”后删除

```js
// ❌ BAD
// 清理时删掉使用者写的注释和 console.log

// ✅ GOOD
保留原注释和日志；只有用户说“删掉这段注释 / 去掉这个 console.log”时才动
```

“帮我清理无用代码”“review 并清理样式”“这些应该没用了”都不构成许可。必须出现明确对象，例如：“删除画布字段上那三条注释”。
