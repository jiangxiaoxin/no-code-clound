---
description: 未经使用者批准，不要用浏览器工具做测试
alwaysApply: true
---

# 浏览器测试须经批准

不要自动调用浏览器工具做测试、验证或点页面。只有使用者明确指导并批准时才可以。

## 要求

- 包括 Cursor 浏览器标签、`cursor-ide-browser`、以及 `browser_navigate` / `browser_click` / `browser_snapshot` / `browser_take_screenshot` 等同类工具。
- 改完界面后不要自己打开本机页面去点、截图、当收尾验证。
- 批准必须点名要用浏览器，并说明测哪一段。例如「用浏览器测关联子表单占整行」。
- 「验证一下」「测一下」「做完了」「按规则在浏览器里确认」都不够。

未获批准时：看代码、跑单测，或等使用者本地点过。不要为了补验证去开浏览器。

```text
# ❌ BAD
改完宽度后自己打开 localhost，点进设计器截一张图就声称测过了

# ✅ GOOD
改完后说明改了什么；等你说「用浏览器测工厂表关联子表单」再打开页面
```
