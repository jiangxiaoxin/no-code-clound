---
description: 删除任何文件之前必须先得到用户确认
alwaysApply: true
---

# 删除文件前先确认

删除任何文件之前，必须先列出要删的路径，并得到使用者确认。没有确认就不能删。

## 要求

- 包括 Delete 工具、`rm`、`del`、`Remove-Item`、`git rm`、移入回收站、清空后相当于删掉文件。
- 确认必须点名具体路径。使用者说「删掉刚才那两个 diff」可以；只说「清理一下」「这些没用了」「乱码文件不该留」不行。
- `.superpowers/`、别的 agent 的工作区文件、未跟踪文件，同样要先问。不要因为「是临时文件」或「编码错了」就直接删。
- 用户自行添加的日志仍按「禁止删除用户日志」：即便使用者同意清理项目，没有点名日志文件也不能删。

```text
# ❌ BAD
发现乱码快照，直接 Delete 掉 review-task-1-wip.diff

# ✅ GOOD
准备删除 `.superpowers/sdd/review-task-1-wip.diff`，等你确认后再删
```
