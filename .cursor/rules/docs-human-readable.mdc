---
description: 测试用例、使用说明等面向人的文档要准确且易懂，按界面操作写，少用规格黑话
alwaysApply: true
---

# 面向人的文档要写得让人看得懂

`docs/testcases/`、`docs/guides/` 以及同类**给人对照界面执行或阅读**的文档，读者是测试同事、产品、实施同学，不是解析器，也不是只看 spec 的开发者。

**准确和易懂同样重要。** 行为写错不行；行为写对但没人读得懂，也不行。

## 适用范围

- **要按这条写**：手工测试用例、使用说明、操作指引、README 里给执行者看的段落。
- **可以更偏规格**：`docs/superpowers/specs/`、`docs/superpowers/plans/` 里的设计、接口、实现约定；但从中拆到用例或 guide 时，必须改写成界面语言。

## 怎么写

- **步骤**：按用户实际点击顺序写，例如「添加数据 → 订单明细点添加 → 点物料 → 勾选 3 条 → 确定」。不要只写内部状态名（「当前行为空」「未就绪」）而不说界面上长什么样。
- **配置**：写清在设计器**哪一块、哪个按钮、选什么**。需要字段 `type` 时，**调色板中文名和 type 一起写**（如「选择数据 `data`」），不要只剩 `optionFilters`、flatten 之类实现词。
- **条件**：若步骤依赖某种配置，说明**怎么配、不配行不行**。例如「过滤条件不要绑 `订单明细.xxx`；不配过滤也算满足」——不要只写「过滤条件不引用子表列」。
- **期望**：写用户能看见的结果（几行、什么文案、什么提示），不要只写存储形状（存 id、写 patches）除非测的就是入库。
- **一条用例一件事**：步骤列只放本编号要点的操作；背景配置放到「预备」或表结构，或表前简短说明。

## 禁止

- 把 spec 里的段落原样贴进用例，指望读者自己翻译成点击路径。
- 用实现术语当步骤主语，却不解释对应哪个界面控件（如单独写「过滤条件不引用订单明细列」而不说在「选择过程设置」里怎么配）。
- 写出与当前界面或实现不符的操作或期望（不一致时改文档或改代码，不要留过期说法）。

```text
# ❌ BAD（准确但人看不懂）
过滤条件不引用「订单明细」列；当前行为空；一次勾 3 条确定

# ✅ GOOD（对照界面能照着点）
添加数据 → 订单明细点添加得 1 行 → 确认物料没配「过滤条件 = 订单明细.某列」（不配即可）
→ 该行物料还空 → 弹窗勾选 3 条设备台账 → 确定

# ❌ BAD
回显=设备编号（子表选择数据根本没有这项配置）

# ✅ GOOD
数据源=设备台账；子列没有「显示在表单中的字段」；选中后本格显示「已选择」
```

改旧用例时发现规格黑话，顺手改成界面语言，不要只在新文件里遵守。
