---
description: 设计文档、实现计划、测试用例等须用人话写，禁止只用规格黑话
alwaysApply: true
---

# 文档用人话写，不要规格黑话

设计文档、实现计划、测试用例、使用说明都是给人读的：产品、测试、实施、以后接手的开发。不是给解析器看的。

**准确和易懂同样重要。** 行为写对但没人读得懂，等于没写好。

## 适用范围

下面这些**一律按这条写**，没有「设计/计划可以更偏规格」的例外：

- 设计文档：`docs/superpowers/specs/`
- 实现计划：`docs/superpowers/plans/`
- 测试用例：`docs/testcases/`
- 使用说明：`docs/guides/`
- 同类给人阅读的说明（如 `docs/testcases/README.md`）

接口路径、库表字段、源码文件名可以出现，但**不能只写这些**。先写界面上叫什么、人怎么点、会看见什么；需要对照实现时再括注文件或 `type`。

## 怎么写

- **步骤**：按实际点击顺序写（「数据管理 → 新增 → 订单明细点添加 → 点物料 → 勾选 → 确定」）。不要只写内部状态（「当前行为空」「未就绪」「条件未引用子表列」）而不说界面上怎样。
- **配置**：写清在设计器哪一块、哪个按钮、选什么。需要字段 `type` 时，**调色板中文名和 type 一起写**（如「选择数据 `data`」），不要只剩 `optionFilters`、flatten、`valueType === 'field'`。
- **条件**：说明怎么配、不配行不行。例如「过滤条件不要绑订单明细的某列；不配过滤也算满足」。
- **期望 / 完成标准**：写用户能看见的结果（几行、文案、提示、图标），不要只写存储形状（存 id、写 patches）除非文档就是在约定入库。
- **计划里的任务**：可以说改哪个文件，但「做什么 / 怎样算做完」仍用界面语言，不要整段只列函数名。
- **一条用例一件事**：步骤列只放本编号要点；背景配置放到「预备」或表结构。

## 禁止

- 用实现术语当主语，却不解释对应哪个界面控件。
- 把只有开发能懂的段落原样当作用例或设计说明（如单独写「过滤条件不引用订单明细列」而不说在「选择过程设置」里怎么配）。
- 写出与当前界面不符的操作或期望（不一致时改文档或改代码）。
- 设计、计划、用例互相复制黑话而不改写成界面语言。

```text
# ❌ BAD
过滤条件不引用「订单明细」列；当前行为空；一次勾 3 条确定

# ✅ GOOD
添加数据 → 订单明细点添加得 1 行 → 确认物料没配「过滤条件 = 订单明细.某列」（不配即可）
→ 该行物料还空 → 弹窗勾选 3 条设备台账 → 确定

# ❌ BAD
回显=设备编号（子表选择数据根本没有这项配置）

# ✅ GOOD
数据源=设备台账；子列没有「显示在表单中的字段」；选中后本格显示「已选择」

# ❌ BAD（设计 / 计划）
子字段 optionSource=linkage 且 isLinkageConfigured 时 hasLinkage 为真，loadRowLinkages 按 rowValues 查 sourceFormId。

# ✅ GOOD（设计 / 计划）
订单明细某列取值来源选「数据联动」并保存后，填报时该行填了条件里的列（如物料编码），同一行目标列（如物料名称）带出源表的值。实现落在 FormSubform 的行内查询。
```

改旧文档时遇到规格黑话，顺手改成界面语言，不要只在新文件里遵守。
